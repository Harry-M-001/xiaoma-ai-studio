# 更新日志

本项目遵循「一个版本 = 一个 git tag + 一份 GitHub/Gitee Release」。应用内的「系统设置 → 关于与更新」读的就是 Release 里的说明。

## v1.1.29 · 2026-09-21

批次 9 的第一块地基：**本机引擎**（`#40`）——一份清单、一台机器的体检、一条下载校验链。到这儿「本地跑还是调 API」这件事**第一次变成用户能自己选的一个页面**，后面几档能力（本机配音、超分、补帧、去字幕高质量档）都从这一页往下接。

### 这一版做了什么

- **「本机引擎」页**（侧边栏「设置」组，在「模型服务」之后）：每一项都写清**用途、体积、授权、下载地址、以及这台机器上跑不跑得动**。
- **清单里 6 个引擎**（体积与 sha256 逐项核对过，见下）：配音运行时（sherpa-onnx，Windows x64 静态 CRT 版）+ Kokoro 中文模型 + ZipVoice 音色克隆模型；超分两档（Real-ESRGAN / waifu2x）；去字幕高质量档（VSR）。
- **一条真的下载链**：断点续传 → 比对 sha256 → 解压（zip / tar.bz2）→ 检查「装好了没有」→ 可停、可删、可重新校验。
- **硬件分档的实话**：有独显且 Vulkan 可用 → 推荐本机跑；没有 Vulkan → 超分那两档**按钮直接灰掉并写出为什么**；配音那几档纯 CPU 就能跑，所以没显卡也推荐。
- **「你已经有的本机能力」**：复用既有的 Ollama / ComfyUI 判据，不另写一套。你有 ComfyUI 的话，超分与补帧可以先走它，不必再下这几档。

### 三处与路线图记的不一样（探针实测纠正）

| 项 | 路线图原来记的 | 实测 |
| --- | --- | --- |
| waifu2x 的仓库 | `nagadomi/waifu2x-ncnn-vulkan` | **该仓库已取不到（404）**；现在在 `nihui/waifu2x-ncnn-vulkan`，2025-09-15 仍在发版 |
| Kokoro 模型的体积 | 约 327MB | **int8 量化版只要 140MB**（147,031,220 字节），fp32 版才是 348MB |
| Real-ESRGAN 的 windows 包 | 在 latest 上 | **latest（v0.3.0）一个资产都没有**，包在 `v0.2.5.0` 那个 tag 上 |

### 体积与 sha256 从哪来（这一版最要紧的一条口径）

**只能来自上游，不能来自印象。** 每个数都带一个出处：

- 5 个取自上游 release asset 自带的 `digest`（`sha256:…`）——字节数与哈希同一个来源，不会出现「体积是新的、哈希是旧的」这种错位；
- 1 个（Real-ESRGAN，上游没给 digest）**自己下载回来算**，并且拿上游报的字节数对了一遍：**45,474,481 字节**，sha256 `abc02804e1…`。

探针里我凭印象写过一个期望体积（比真实值大 5 万字节），结果是「永远下不完」，白折腾了一轮。所以清单里每个数都带 `proof` 字段写清出处。

### 两个只有真跑才知道的坑

1. **`416 Range Not Satisfiable` 的意思是「本地这份已经完整」。** 断点续传发 `Range: bytes=N-`，起点落到文件末尾之后时服务器就回 416——那是「不用下了」，不是「出错」。探针里我把它当错误处理，**把一份已经下好的 43MB 文件删了重下**。现在：收到 416 就去校验本地那份。
2. **`.tar.bz2` 是流式压缩，看目录结构要喂前缀。** 想确认包里有没有我们要的那个 exe，不必下 249MB：前 4MB 就能把 tar 的头解出来（实测拿到了 `…/bin/sherpa-onnx-keyword-spotter.exe` 这样的条目）。而那个 exe 在包里很靠后，所以改去读了**打包它的 workflow**（`windows-x64.yaml` 里写着 `EXE=sherpa-onnx-offline-tts.exe`，并且另外打了 `-no-tts` 变体）——判据有出处，不是猜的。

### 判据只认文件名，不认目录层级

「装好了没有」用的是**文件名级通配**（`sherpa-onnx-offline-tts.exe`、`*.onnx`、`waifu2x*.exe`），刻意不写死上游的目录结构：上游哪天把 `bin/x.exe` 挪成 `x.exe`，写死路径的表现就是「装完了界面上还说没装」。解压时会**把只有一层顶层目录的包拆平**，所以两种布局都能用。

### 顺手做的三件事

- **虚拟显示适配器要过滤**：这台机器上有一个投屏软件装的 `OrayIddDriver Device`，不过滤的话界面上会写「检测到独立显卡」，而用户按这句话去装超分、跑起来才发现用的是集显。
- **Vulkan 检测看两件事**：加载器（`vulkan-1.dll`）在 **且** 有驱动登记的 ICD（或 `vulkaninfo` 读得到设备名）才算可用——只看加载器会骗人，它几乎总在。
- **后端给界面的文字是纯文本**：React 不渲染 markdown，卡片说明里写 `**粗**` 就是把星号显示给用户（这一条是浏览器走查逮到的，已加用例钉住）。

### 验证

- **真跑（在这台机器上，RTX 5060 + Vulkan）**：① 下载 Real-ESRGAN 43.4MB → 校验 → 解压 → **真跑一次超分**，160x90 → 320x180；② 从浏览器点「接着下载」把 waifu2x 33.9MB **从断点续传下完**，sha256 与清单一致，`waifu2x-ncnn-vulkan.exe` 命中，**真跑出图**。
- **清单核对**：6 个地址逐个与上游的 `browser_download_url` 比对，体积与 sha256 全部一致。
- 后端单测 **952 项全绿（58 个文件）**：新增 `test_local_engines.py` **38 项**——清单自检、硬件分档（没 Vulkan 就必须拒绝）、标志文件按文件名匹配、以及**用一个本地 HTTP 服务器**跑完整的下载链（完整下载、校验不过要删文件、断点续传真的发了 Range、**416 视为已完整**、路径穿越要挡住、缺标志文件要报错且不留半个目录、同一时间只允许一条、停止要留断点、删除要收干净）。
- 无数据库迁移之外的结构改动：新增 `0016_engines_nav`（老库补上导航项，只挪仍是旧默认值的行）。

## v1.1.28 · 2026-09-21

数字人这条线的最后一块：**一镜里换好几个人说话**——两个人对答写在同一镜里，各用各的音色。到这儿 `#39` 数字人收口。

### 为什么「按行切」不够

这个需求听着是「每一行换一个音色」，但分镜表里最常见的写法是**一行写两句**：「小焰：你终于来了。老陈：我不走。」按行切的话整行只能用一个音色，另一个人的台词会用别人的嗓子念出来——而用户看到的只是「音色没生效」。

所以按**句**切：

- 一行里第二句**自带的「老陈：」标签也会换人**（判据与行首完全一样：短标签 + 冒号），而且那个标签不会被念出来；
- 说话人的继承规则与以前**完全一致**（这一行写了标签就以它为准，没写就跟着上一位；认不出任何标签时说话人是空的，由调用方退回默认音色——**不猜**一个角色出来）。

### 三个决定

1. **相邻同音色的句子并成一段**：同一个人连说三句只合成一次；「说话人不同但音色相同」也并（念出来是同一个嗓子，分两段没有任何好处）。少花两次钱，也少两处接缝。
2. **段与段之间补 0.15 秒静音**：不同音色的两段直接对接时，上一句的尾音会和下一句的头音咬在一起（各家 TTS 首尾静音长度不一样），听着像一个人在抢话。代价是**拼完比各句之和略长**。
3. **上游只认一条参考音频**，所以必须在本地拼成这一镜的**一条**：每句各合成一次 → `join_audio` 拼起来 → 只把拼好那一条登记成资产。中间那几句**只落临时目录、不登记资产**，否则资产库会多出一堆「配音 · 你」这种垃圾条目，而用户要的是这一镜的成品那一条。

### 顺带修掉的三处（都是这一版自己身上的）

- **判据必须量拼完的长度**：多出来的那点静音会真的改变「这一镜读不读得完」的结论——两句各 1.0 秒，分头算是 2.0 秒（塞得进 2 秒的镜），拼完 2.15 秒就塞不下了。有一条用例专门钉住这个差别（量错的话这一镜会被派出去，用户拿到的成片是第二个人话没说完，而钱已经花了）。
- **「一条都没派」原来会指错地方**：整镜都被对白这一关拦下时，报的却是「分镜图与镜头对不上……而分镜表要的是镜号 。」——图和镜号其实是对的，用户会跑去反复核对镜号。现在两种原因分开说（对不上 / 每一镜都被对白拦下），同时出现就两条都说。
- **中间音频的文件名少了那个点**（`seg0mp3`）：拼接时 ffmpeg 靠**内容**也能认出格式，所以它不报错、也就容易一直错下去，而「按扩展名判断」是后面任何一处都可能做的事。用例守着。

### 其它

- **预览里记的是「要合成几次」**（原来是 `True`）：一镜多人的账是每句一次，记成布尔会把这一跑的账少算；确认弹窗里也补了一句说明——「一镜里有几个人说话时，每一句各合成一次、再拼成这一镜的一条配音」。
- 画布视频节点的「逐镜对白」提示里写清了同行的写法与示例（「小焰：你终于来了。老陈：我不走。」）。
- 无数据库迁移。
- 后端单测 **915 项全绿（57 个文件）**：`test_digital_human.py` +4（含整链真跑 ffmpeg——替身只替「合成」那一步，拼接、量时长、登记资产都是真的）、`test_speech.py` +1、`test_frontend_speech.py` 的契约用例改成看两层（这一版把「合成」与「落库」拆开了，只看一处会漏掉真正回给前端的东西）。浏览器走查：节点面板上的新提示渲染完整、没有被裁掉（它所在的浮动面板可滚动，提示自身 86px 高、不裁切，底部「运行此节点」照旧可见）。

## v1.1.27 · 2026-09-21

批次 8 第八项：**去字幕**——有的视频模型会在画面里烧上一行字幕，效果往往不如自己后期配的。这一版把那一行去掉，好让你配自己的。

**先说清它做不到什么**：这一步是**盖掉像素**，不是把字「擦掉再补回原来那张画」。ffmpeg 能做的四种手法都会在字幕那一带留下痕迹，只是痕迹的样子不同。真正无痕要 AI 补全（那是批次 9 的另一档，`#44` 本机 VSR，需要自己下载 731MB 的运行环境）。

### 四种手法，每一条都是实测出来的

在 640x360 上造「本来就没字」与「烧了字」两版，对**不改画面几何**的手法量字幕区 PSNR（越高越接近「从来就没字」）：

| 底图 | 不去 | 抹平 | 遮住 | 模糊 |
| --- | --- | --- | --- | --- |
| 竖直渐变（天空 / 墙 / 虚化） | 17.5 | **41.6** | 33.5 | 27.4 |
| 纹理密集 | 14.8 | **20.3** | 17.9 | 16.8 |

- **抹平**（`delogo`）：按框四周的像素推回来填。**平整背景上几乎看不出来**（17.5 → 41.6 dB），背景有横向色块或细密纹理时会留下竖向拖痕。
- **遮住**：把紧邻的一条画面拉过来盖住。画面尺寸不变，任何位置都能用。
- **模糊**：把这一条糊掉。最稳，但那条糊痕一定在。
- **裁掉**：那一条直接不存在了，**最干净**——代价是画面会变矮（1080p 裁掉 15% 就是 918p）。裁掉改了几何，跨几何比 PSNR 没有意义（实测过：裁掉拉伸在字幕区里反而「更不像」，因为整幅都错位了），所以这一条只能靠眼睛看，界面上给的就是真帧对比。

### 三个「只有真跑才知道」的约束

1. **抹平的框不能碰画面四边**：碰了 ffmpeg 直接 `Conversion failed`（实测 x=0、y+h=画面高都失败，**留 1px 就成**）。字幕贴着画面底边是最常见的情形，所以这里自动内缩 1px，并在界面上说清「那一行原样保留」——不缩就用不了。
2. **零宽 / 零高的框不报错**：实测 `delogo=w=0` 退出码 0，也就是「成功且什么都没做」。所以最小边（8px）由我们自己拦，不指望 ffmpeg。
3. **裁掉只有贴着画面上下边缘时才是一刀切干净**。框在画面中间时**不让用**，并说明原因：把中间一条挖掉再上下对接，画面的上下两条会错位——那比留一条糊痕更糟。

### 怎么用

时间线上任一片段点「去字幕」：

- **在真帧上拖框**（不是填坐标）：字幕在画面哪儿只能看。框的坐标全程是**源视频像素**，显示尺寸只影响画法。
- **先看真渲染的一帧**：预览是后端把同一帧跑一遍**同一套滤镜**回来的图，左右并排对比原帧与处理后。与字幕版式预览同一条口径——「预览看着行、导出来不行」最伤信任。
- **框架一变，可选项跟着变**：框挪到画面中间时「裁掉」就不能用了，界面会灰掉它并写出原因（而不是等你点了才报错）。
- 满意了点「开始去字幕」：**产物是新资产**，时间线上那一段换成它，**原片不动**（与产物版本栈、候选定稿同一条口径）。

### 说明（边界与顺手修掉的东西）

- **词表/字幕那条链上的两处旧账**：① 烧字幕的临时工作目录以前不收（`storage/tmp` 下积了 14 个 `subs-*`，里面是 `fontsdir=.` 要求放进去的字体硬链接）——现在成功失败都收走，并有用例守着；② 清尾时还发现 13 个**没登记的孤儿视频文件**（历次验证留下的），已清掉。
- 无数据库迁移；新增 `services/subtitle_removal.py`（纯逻辑：框几何、四种手法、滤镜参数、能不能用与为什么）、`ffmpeg_service` 三个函数（取帧 / 真渲一帧 / 处理整段，音轨 `-c copy` 原样带过去）、四个接口（取帧 / 问可选项 / 预览 / 执行）。
- **浏览器走查抓到三个真问题**（都在这一版自己身上）：① **「1px 压扁」这个坑又踩了一次** —— `.subrm-canvas` 写了 `overflow: hidden` 又放在可滚动 flex 列里，被压成 1px，等于**框选的那块画面看不见也拖不动**（比上一版那个「点不动」更严重）；修好之后把规则写成了用例守着。② **拖动之后可选项还是旧的** —— 界面说「裁掉能选」、后端说不能，于是加了只算几何、不碰 ffmpeg 的 `/check`，松手就刷新。③ **快速一甩会把框弹回去** —— 按下/移动/松手都发生在同一帧时，松手那一刻闭包里的框还是旧值，刷新可选项时把旧框设了回去；改成用 ref 同步保存当前框。
- 后端单测 **901 项全绿（57 个文件）**：新增 `test_subtitle_removal.py` 14 项（含真跑 ffmpeg：三种框位 × 四种手法共 10 种组合都要真渲出来，且只有裁掉会改尺寸）、`test_frontend_subtitle_removal.py` 10 项（含「最小边前后端同数」与「可滚动 flex 列里的盒子不许塌」两条元测试）。另有真跑核对：真建项目走一遍接口 10 项（含「字幕带内 PSNR 14.1 dB、带外 41.3 dB」）、浏览器整条用户路径走查（取帧 → 拖框 → 换手法 → 看真帧对比 → 处理 → 时间线换上新片段）。

## v1.1.26 · 2026-09-21

批次 8 第七项：**运镜预设词表**——35 种运镜、10 个分组、183 种写法，一份表同时管住分镜提示词、解析、体检和体检建议。

这一项要治的是一个**看不见的毛病**：分镜表里写「跟拍」，样片却按推近动了。

### 病根：同一个概念，四份词各写一遍

| 位置 | 以前 | 后果 |
| --- | --- | --- |
| 分镜提示词 | 只写「禁止写『推拉摇移』组合」，**从没告诉模型该用哪些词** | 模型自造「跟拍 / 缓推 / 推轨」 |
| 静图样片的解析 | 自己一份 `_MOVE_RULES` | 认不出 → **静默套一个推近**：样片动了，动的不是分镜表写的意思 |
| 分镜体检 | 自己一份 `_MOVE_HINTS` | 同一份分镜表，体检说「不是固定镜头」、样片却一动不动 |
| 体检的建议文案 | **手抄**一份中文名 | 推一个词表外的词给用户，照着改还是错 |

前三条合起来的效果是：**错了不报错，只是拍出来跟写的不是一个东西**。现在收敛成 `services/camera_moves.py` 一份表，四处都从这里取。

### 新增

- **35 种运镜**（10 组）：固定 / 手持微晃 / 慢推·快推·急推·变焦推·变焦推近 / 慢拉·拉远揭示·急拉·变焦拉远 / 左摇·右摇·横摇扫过·甩镜 / 左移·右移·跟拍·推轨 / 上摇·下摇·俯仰揭示 / 升镜·降镜·升降 / 航拍上升·下降·跟拍·环绕 / 环绕左·右·弧线·螺旋上升 / 翻滚·移焦。每条都带**用途说明**（「固定：对话戏与情绪停顿的底子」）与**样片降级说明**（「移焦：样片做不出虚实变化，近似成不动——这一条要靠真视频」）。
- **体检多了两条规则**，而且分开说，因为**用户要改的地方不一样**：
  - `move_not_a_move`：写的是**机位 / 镜头类型**（「主观镜头」「过肩」「空镜」）——该挪到画面描述里，运镜栏另选一个运动方式。只报「不在词表里」的话，用户会去词表里挑一个最像的（把「主观镜头」改成「跟拍」），问题其实在这句话待错了栏。
  - `move_unknown`：真正的自造说法（「镜头缓缓飘过」），给一个最接近的词表名。
  - 两条规则的建议文案**从词表生成**，不再手写——手写的那份必然与表脱节。
- **同一种运镜的不同写法，现在算一种**：「缓推 / 推近 / 推进」连着三镜，以前那条「运镜雷同」的规则**看不出来**（三份原文三个 key）；现在按词表的 key 归一，会直说「连续 3 个镜头都是「慢推」（你写的是 推近、推进、缓推）」。体检弹窗里的概览同样按规范名归类（`慢推×3 主观镜头×1 镜头缓缓飘过×1`），认不出来的写法**原样保留**，好和下面那条提醒对得上。
- **体检弹窗里多了一份「运镜词表」**（默认收起，只在有运镜相关提醒时出现）：分组列出全部合法写法，每个词悬停看用途，最后一行是**「不是运镜」**那些词。取不到就直说，并说明体检结果不受影响。

### 匹配规则：单字别名只准兜底

「飘移推近」一度被判成**右移**——单字别名「移」在第 1 个字上命中，而真正表示运镜的「推近」在第 3 个字上，位置优先反而判错。所以规则分成两层：**先用多字别名判（位置优先、同位置取更长的），一个都没命中才拿单字兜底**。

这条比原来「按表内顺序」那条稳：顺序敏感意味着**改表的顺序会改识别结果**，是个埋着的坑。

### 词表靠代码附加，不写在提示词里

这是这一版**最容易做错的一处**：提示词种子数据（`ensure_seed`）**从不覆盖已存在的行**。所以任何只写在种子里的词，对**已经装过、正在升级**的用户都不存在——而他们正是多数。实测确认过：升级后的库里那份分镜提示词仍是旧文本，一个字都没变。

所以词表由代码接在系统提示词**末尾**（`doc_service.HARD_RULES`，与「风格注入」同一个机制）。另外这也更对：它是**与解析的契约**，不是风格偏好——用户当然可以改分镜提示词，但把词表改没了会静默降级。代价写在注释里：系统设置的「创作 Agent」看不到这一段，所以提示词正文里明说「词表由程序附加，不在这里维护」。

### 说明（边界写清楚）

- **词表管的是「运动方式」，不是「机位」**。3D 导演台那 14 个预设是**机位**（静止位置），两件事，没有合并。词表里也不收「主观镜头」这类——它们进了「不是运镜」清单，体检会点名。
- **静图样片做不出效果的运镜会降级**，而且**降级说明会显示出来**（「移焦」「变焦推」「手持微晃」等）：不说的话，「移焦在样片里没做出来」看着就像坏了。
- **顺手修掉两处**：① 体检文案里「「慢推」（你写的是 缓推、推近）」（有一处引号错位，是上一版重构时弄丢的）；② 打包脚本里「随包字体约 82 MB」是旧数（现在只有思源黑体一款，约 32 MB）。
- **浏览器走查抓到一个真 bug**：词表盒子写了 `overflow: hidden`，而 `.lint-dialog-body` 是 flex 列——两者一撞 `min-height: auto` 失效，在一份**内容长到需要滚动**的报告里它被压成 **1px**：标题看着还在，命中区已经没了。也就是说**越是有问题要改的用户越点不开词表**。修法是 `flex: 0 0 auto`，并在前端用例里钉住（这种「看着在、点不动」的毛病不看真浏览器发现不了）。
- 无数据库迁移；新增 `services/camera_moves.py`（纯标准库）、`GET /api/meta/camera-moves`；`doc_service` 新增 `HARD_RULES` / `apply_hard_rules()`，`runner.py` 在生成路上接上（有用例钉住「接了没」——这类「加一层」的功能最容易辅助函数写完、调用点忘了接）。
- 后端单测 **875 项全绿（55 个文件）**：新增 `test_camera_moves.py` 18 项、`test_frontend_lint.py` 2 项；`test_storyboard_lint.py` 的规则覆盖表同步了两条新规则。真跑核对：接口 5 项、真建项目跑体检 8 项、浏览器走查（含上面那个 1px 的坑）。`tsc --noEmit` 与 `vite build` 通过。

## v1.1.25 · 2026-09-20

批次 8 第六项的后半：**随画风的字幕版式**——把字幕烧进成片，10 款版式随你挑，中文字体内置一款、其余按需下载。

这一版顺带把两件一直缺的事补上了：**README 有了「环境要求」章节**（要装什么、去哪下、怎么核对），以及**环境体检开始查 ffmpeg**（含字幕要用的 libass）。

### 字幕：版式由你直接定，画风只给推荐值

这是一个**设计上的修正**。原本打算「字幕版式完全跟画风走」（选了水墨就自动用楷体、选了赛博朋克就自动用花字），用户看过之后否掉了：版式该是一等公民，用户能自己选才最好。

所以做成了这样：

- **10 款版式任选**：纪录片下三分之一 / 居中极简 / 电影感大字 / 综艺花字 / 新闻条 / 宋体文艺 / 古风楷体 / 对白式 / 标题美术字 / 顶部说明。
- **画风只提供默认值**：没自己选过时用画风的推荐值（水墨 → 古风楷体、赛博朋克 → 综艺花字、纪实 → 下三分之一…）；**一旦你选过某款，换画风不会再动它**。
- 字号可调（0.5–2 倍）。

> 这条「显式选择不被别的东西推翻」的口径，与产物版本的回滚（v1.1.22）、候选图的定稿（v1.1.23）是同一条。第三次用到它了——只要用户明确说过一次，就不该被后续操作悄悄改掉。

**预览是真渲染一帧，不是前端近似画。** 版式的效果取决于 libass 的字体选择、字形、描边、缩放、边距；前端再写一套必然对不上，而「预览看着挺好、导出来不一样」是最伤信任的一种不一致。预览图里刻意带上繁体（漢語）、扩展 A 区汉字与英文数字——只写「示例」两个字的话，字体回退和缺字都看不出来。

**时间轴跟着转场走。** 每段填一句话即可，时间码不用管：字幕区间按**成片**里各段的区间算。这点必须做对——配了叠化之后成片会变短（三段各 5 秒 + 1 秒转场 = 13 秒），按「各段时长累加」算的话第 i 段会提前 `转场时长 × i`，第三段整整早 1 秒，用户看到的是「字幕比画面早出来一截」。相邻两条字幕的区间也不重叠（每段的止 = 下一段的起点），转场处不会两行字叠在一起。

### 中文字体：内置一款，其余三款按需下载

**烧中文字幕开箱就能用**——思源黑体（Regular + Bold，约 33 MB）随程序分发。另外三款不随包分发：

| 字体 | 体积 | 用途 |
| --- | --- | --- |
| 思源宋体 | 23.4 MB | 宋体 · 人文 / 叙事 |
| 霞鹜文楷 | 24.4 MB | 楷体 · 古风 / 手写感 |
| 得意黑 | 2.5 MB | 标题美术字（只覆盖通用规范汉字表 8105 字，生僻字会缺） |

在字幕区点一下就地下载。四条实现上的讲究：

- **下到数据目录**（`data/fonts/`），不进程序目录——所以不进 git、不进便携包，而且**重装应用或换便携包时不用重下**；便携包可能落在只读位置，写数据目录才稳。
- **必须比对 sha256**。不比的话，「下到半截的文件」会一路走到渲染，最后表现成「中文变豆腐块」——那时没人会想到是下载坏了。
- **先写 `.part` 再改名**，中断或校验失败不会在字体目录里留下半截文件（而半截文件在「文件在不在」的判断里是「已存在」）。
- **双源回退**：先 Gitee 后 GitHub。地址挂在独立的 `fonts-v1` Release 上而**不是版本 Release**——地址写在代码里，必须长期不变。

四款字体都是 **SIL OFL 1.1**，允许再分发；授权原文与每个文件的 sha256 见 `backend/assets/fonts/SOURCES.md`。

**一条加到字体清单里的硬规矩**：`Fontname` 必须逐字匹配字体**内部**的 family name，不能写文件名——写错时 libass 会**静默回落系统字体**（中文直接变豆腐块），而 ffmpeg 不报错、退出码 0。实测活样本：得意黑的文件名是 `SmileySans-Oblique.ttf`，族名却是 `Smiley Sans Oblique`，按 `Smiley Sans` 写时回落到 `ArialMT`。所以清册里的族名全部是**从字体文件里读出来的实测值**，并且有一条测试逐个核对。

### README 新增「环境要求」章节

原来只在快速开始里写了一句「需要 Python 3.11+ 和 Node.js 20+」，没有下载地址、没有核对办法，**ffmpeg 更是一个字都没提**——而导演台和字幕都依赖它。现在有了：

- 第一张表按**安装方式**分（便携包只要 ffmpeg / Docker 都不要 / 源码方式要 Python + Node + ffmpeg）。
- 逐项写清用途、下载地址、核对命令与常见坑（Python 要勾 Add to PATH、别用微软商店版、别用 ComfyUI 那个没有 `venv` 的嵌入式版；npm 换源；ffmpeg 五个平台的安装命令）。
- **专门写清「烧字幕要 libass，别用裁剪版」**，并给出核对命令——裁剪版 ffmpeg 少了 `ass`/`subtitles` 滤镜，而截取、合并、转场看起来全都正常，只有烧字幕时才失败。
- 可选组件（Ollama / ComfyUI / 三款字体）各自的地址与接入方式。

### 环境体检开始查 ffmpeg（含 libass）

`backend/tools/env_report.py` 原来只查 Python / Node / venv / 端口 / 数据目录，**ffmpeg 完全没查**——用户装不上时它一句话都不说。现在新增三项检查，并加了 `--hint ffmpeg-missing` / `--hint ffmpeg-nolibass` 两条可直接展示的安装指引。

补的时候撞到两个真问题：

1. **只看 `shutil.which` 会误报。** 真实现场：PATH 里那个 ffmpeg 是某软件自带的裁剪版（没有 libass），而应用自己会跳过它、用 winget 装的完整版。体检只看 PATH 的话，用户会看到一个根本不存在的警告。现在**候选全找一遍、挑带 libass 的那个**，并把「另一份没有 libass、应用不会用它」也如实报出来。
2. **`ffprobe` 必须与选中的 `ffmpeg` 成对。** 原来它单独走 PATH，结果报出来是「ffmpeg 用 winget 的、ffprobe 用另一个的」——两个本来一起发布的工具版本不搭会出怪问题。现在先看同一个 `bin` 目录里有没有，再退回 PATH。

### 顺手修掉的三个真 bug（都是走查/真跑逮到的）

| 问题 | 表现 | 修法 |
| --- | --- | --- |
| **下载成功后界面不刷新** | 接口只回了字体列表，而版式里的「这款字体在不在本机」还是页面加载时那一次的老值 → 「下完了但提示还在」 | 下载接口回**整份选项**，前端整体替换（只刷新一半是这类 bug 的温床） |
| 判断「字体已装好」只看文件在不在 | 字体被截断成 1KB 时它回「不用再下」，然后拿着坏文件去渲染——libass 静默回落系统字体，出一部字体不对的片子 | 改走校验和，「没下过」与「下坏了」分开说（后者要用户重新下载） |
| 选了字体没下的版式会去渲预览 | 后端拒绝、弹一句错，而那时用户该做的是下载 | 字体没就绪就不去渲预览，直接给他下载入口 |

### 说明（边界写清楚，别过度承诺）

- **要烧字幕就绕不开 ffmpeg**：它不在便携包里（各平台装法差异大、体积也大），但**不装也不影响启动与对话、出图、出视频**，只影响导演台 / 静图样片 / 字幕。
- **字幕是烧进画面的**（硬字幕），不是可开关的外挂字幕——所以导出前请确认版式，导出后改不了。
- **得意黑只覆盖通用规范汉字表 8105 字**（简体），繁体与生僻字会缺；它是标题字体，不建议拿它排正文。其余三款都覆盖 GB18030 与繁体。
- **不用可变字体（variable font）**：libass 0.17 不支持 OpenType 可变字体的实例选择（带 `Bold` 位也只会渲染成 Regular），Adobe 还警告 CFF2 可变字体在部分 Windows 10/11 上会导致文字损坏。所以清册里全是静态实例。
- **ASS 里没有真正的竖排**，只能靠逐字换行近似、标点仍按横排处理，所以版式里不提供竖排。
- 无数据库迁移（版式是每次导出时的选择，不挂在风格卡上）。
- 后端单测 **856 项全绿（54 个文件）**：新增 `test_subtitles.py` 49 项 + `test_frontend_director_subtitle.py` 10 项，并给 `test_director_transitions.py` 补了 5 项字幕接线（33 → 38）、给 `test_env_report.py` 补了 7 项（19 → 26）。`tsc --noEmit` 与 `npm run build` 通过。其中有一条**元测试：不许有重名的测试函数**——改测试时撞过一次，两个函数同名时 Python 只留最后一个，另一个**静默不跑**、断言就不执行了。
- **真跑验证（一分钱不花）**：19 项——字幕区间内画面变了 1044 个像素、**区间外 0 个**（时间轴真的对得上，不是一直显示）；换字体后差 2376 个像素（`fontsdir` 真生效、没回落系统字体）；繁简与扩展 A 区汉字零缺字告警；缺字体时被前置拦住。字体下载链也真跑了一遍：下载 → 校验通过 → **把文件改坏 → 能发现校验和不符并重下** → 用它渲染零缺字。浏览器实机走查确认整条用户路径（截两段 → 选古风楷体 → 就地下载 24.4MB → 真渲染预览 → 填两段 → 导出 → 取帧确认两段都烧上了字）。

## v1.1.24 · 2026-09-20

批次 8 第六项的前半：**零成本转场 + 转场音效**——片段之间能叠化/过黑/白闪/横划，转场处能带一声音效，全程不下载任何素材、不多花一分钱。

这一项原本是「转场 + 音效库 + 字幕版式」三件事拼在一起。按**技术耦合**切开做：转场与它的音效共用同一次 ffmpeg 导出（一个滤镜图里做画面过渡 + 音轨交叉淡化 + 音效混入），所以先做这两个；**字幕版式留到下一版**——它要单独处理中文字体那条链（libass/FreeType + 字体文件 + 路径转义），塞在一起会把两件不相干的风险叠在一块儿。

### 新增

- **导演台的「片段之间怎么接」**：硬切（默认）/ 叠化 / 过黑 / 白闪 / 横划 / 圆形展开，六选一。
- **转场时长**：0.2–2 秒可调（超出范围夹到边界，不报错——改一下就好）。
- **转场处音效**：内置四款**本机现场合成**的（呼啸 / 上扬 / 闷响 / 轻点），也可以从资产库里挑一条自己的音频。
- **合并前先算账**：界面上直接写「成片约 13.0s——比硬切短 1.0s」。这一条见下。

### 为什么要把「会变短」写在按钮旁边

`xfade` 是让相邻两段**交叠**，不是插一段新的：三段各 5 秒、转场 1 秒，成片是 **13 秒而不是 15 秒**。这件事如果只写在文档里，用户就是拿到片子发现短了再去猜。所以这一版在合并**之前**就把成片时长和「比硬切短多少」摆出来，而在预览与合并之间用的是**同一套口径**（`services/transitions.py`）——两处各算一次的话，「弹窗里写 13 秒、导出来 12.5 秒」是最难解释的那种不一致。

### 顺手修掉的两个真问题（都是「不报错、悄悄给错东西」那一类）

1. **硬切的「流拷贝直拼」只对同构素材成立，以前不判断。** `concat` demuxer 是把各段的包**直接接起来**、不做任何转换；素材的尺寸 / 帧率 / 编码 / 有没有音轨但凡不一致，接出来的片子时间戳就是错的——**而且命令照样成功返回**。实测三段共 15 秒的素材导出成 **18.75 秒、音轨只有 15.02 秒**，画面与声音对不上，全程没有任何提示。现在只在素材同构时才走这条快路（同源切出来的片段照旧秒出），不同构就逐段归一化后重编码。
2. **同源的老三级回退会把有声音的片段弄哑。** 老的「concat 滤镜 → 丢掉音轨」回退在**有的段有音轨、有的段没有**时，第二级必然失败，然后退到第三级整条丢音轨。现在给缺音轨的段补一条等长静音，音轨就保住了。归一化这一步与转场那条链**共用同一段代码**，免得出现「转场对得上、硬切偏了」这种最难查的偏差。

还有一个是做测试时逮到的：**转场预设的 key 与 `xfade` 的滤镜名不是一回事**（「过黑」的 key 是 `fade`、滤镜名是 `fadeblack`；`wipe` 压根没有同名的滤镜）。把 key 直接当滤镜名会掉进两个坑——名字碰巧存在（`fade` 就是），于是「过黑」**悄悄变成普通叠化**、不报错；名字不存在（`wipe`）才报错。现在一律回过预设表取，并且有一条测试逐个预设钉住。

### 说明（边界写清楚，别过度承诺）

- **转场放不下就不让点**：`xfade` 要求转场时长短于相邻两段。放不下时按钮变灰并直说「转场要 2 秒，而第 2 段一共只有 1.5 秒——放不下」，而不是扔给 ffmpeg 回一句 `Invalid duration`。
- **时长读不出来的片段直接拦住**：转场位置是按每段时长算出来的，算不出来还硬接，画面会错位且 ffmpeg 不一定报错。
- **音效是本机现场合成的，不下载素材**：配方就是几条 lavfi 表达式（粉噪声带通 / 频率上扫正弦 / 低频一击 + 指数衰减），所以零授权风险、零仓库体积、换台机器出来的是同一段声音，也不需要联网。想用真素材就走「从资产库挑一条自己的音频」那条路，混音链路完全一样。
- **音效比转场长一个尾巴**（0.35 秒），否则「呼」的一声会跟着转场一起被切掉；混音以主音轨为准，长音效不会把成片拖长。
- **硬切仍是默认，且不加任何滤镜**：老流程的速度与画质一点没变。
- **转场预设表只在后端有一份**（`GET /api/director/transitions`），前端不另抄——抄一份就可能出现「界面能选出一种后端不认识的转场」，而报错要到合并时才知道。有一条前端静态测试专门禁止在页面里硬编码转场名。
- 无数据库迁移。
- 后端单测 **785 项全绿（52 个文件）**：新增 `test_director_transitions.py` 33 项 + `test_frontend_director_transition.py` 8 项。`tsc --noEmit` 与 `npm run build` 通过。
- **真跑验证（一分钱不花）**：三份素材刻意造成尺寸 / 帧率 / 有没有音轨全不一样，量出来的是——硬切 15.0 秒（修之前是 18.75 秒）、转场 14.0 秒、转场中点取帧是红绿混色（真的在过渡）、选「过黑」时中点是**暗的**（证明滤镜名是对的）、转场处音量 −27.9dB 而别处 −91dB（音效真的混进去了）、不加音效时同一处是 −91dB。接口层走 HTTP 另验 15 项，浏览器实机走查确认「会变短」那句话、放不下时按钮变灰、成片真的落库。

## v1.1.23 · 2026-09-20

批次 8 第五项：**候选选优（定稿）**——一次出多张，挑一张定稿，下游只用那一张。

### 这不是「更方便」，是修了一个真问题

以前批量出图之后，**下游用哪一张由不得你**：

- 逐镜出视频遇到「一镜多张」，只能取**最后生成的那张**（代码里写的就是「同一镜有多张时保留最新的一张」）；
- 普通视频节点首尾帧模式按生成顺序取**前两张**——四张候选里取头两张，与你想用哪张无关。

而且这两处都在界面上完全看不出来：节点照样出片，只是用的不是你要的那张。所以这一版把「挑哪一张」变成一个能说清楚、能存下来的选择。

### 新增

- **产物网格里每张图右上角一个「定稿」**：点了它，下游就只用这一张。再点一次取消。
- **一组候选 = 同一镜 / 同一资产 / 同一个提示词**（按最稳定的身份分组：镜号 → 资产名 → 整节点一组）。所以分镜图节点是**每一镜各挑各的**，普通图片节点是整节点挑一张，互不影响。
- 已定稿的那张有明显标记（描边 + 常显的紫色「已定稿」），鼠标移到哪张都不会看错。
- 面板上会如实说状态：「下游只用已定稿的那张（N 组已定稿）；没定稿的组会把整组都交给下游」。

### 说明（边界写清楚，别过度承诺）

- **一组只有一张时不出现「定稿」按钮**：那是个没意义的动作。
- **定稿是显式的，生成新产物不会推翻它**（与回滚同一个口径）。定稿跟着**镜号**走，所以「定稿 → 又生成一版」之后仍然生效，不用重新挑。
- **定稿指不到当前这一版的图时**（比如回滚到了定稿之前的那一版），那一组**整组交付**——不因为一次选择失效就让下游什么都读不到；面板会明说「有 N 组定稿指不到当前这一版的图」。
- **展示与交付是两件事**：节点面板列的是**全部候选**（不然挑都没得挑），交付给下游的才按定稿收缩。两处共用同一个出口（`_latest_task_assets`），所以不会出现「画布上显示这张、下游用那张」。
- **整图运行同样忽略定稿**（与忽略回滚同一个理由：那一跑节点会被重跑，产出新的候选）。想让定稿生效就单跑下游那个节点。
- 无数据库迁移；定稿存在节点 data 的 `picks`（`{组键: 资产 id}`），跟着图走、分享码也能带走。
- 分组规则**只在后端有一份**（`candidate_key`），由产物视图的 `candidateKey` 带出去给前端用——前端重写一遍会出现「界面上分在一组的、后端其实不是一组」。
- 后端单测 **744 项全绿（50 个文件）**：新增 `test_canvas_picks.py` 20 项。`tsc --noEmit` 通过。
- **真跑验证（一分钱不花，19 项）**：造真候选（真 PNG）、视频走本机假方舟，直接断言视频任务参数里的 `first_frame_asset_id`——图片节点定稿前首帧=第 1 张、尾帧=第 2 张；**定稿第 3 张后首帧=第 3 张、且没有尾帧**；分镜图只给镜 1 定稿时镜 1 用它、镜 2 不受影响；定稿指向不存在的图时那一组退回老行为。

### 一处设计取舍（浏览器走查时改的）

**「定稿」按钮做成常显，而不是鼠标移上去才出现。** 一开始是悬停才显示（格子多的时候更清爽），走查时发现两个问题：不悬停就不知道有这个功能，而它决定下游用哪张——这种事不该藏；而且悬停态在无鼠标 / 触屏上根本不出现。所以宁可让密集的网格稍微满一点，底色做成半透明，鼠标移到它自己身上才变实。

## v1.1.22 · 2026-09-19

批次 8 第四项：**产物版本栈**——同一节点多次生成，每一版都留着，可以回滚。

以前「再生成一次」等于把上一版顶掉：不满意也没法退回去，只能重跑（再花钱）。现在每生成一次就多一版，随时能切回旧的那一版接着往下做。

### 新增

- **节点浮框里的「产物版本」**：一个下拉列出这个节点历次生成，每版显示**第几版 / 件数 / 失败条数 / 时间**，还有前几张产物当缩略图——认版本靠看一眼，标题里的时间帮不上忙。
- **回滚**：选中旧版本，下游读到的就是那一版。
  - 「下游」包括**整条链**：单跑下游节点、整图预览、分镜体检、静图样片，读的产物都从同一处取，所以只要这里认了，全链都认。
  - 回滚只改「交付哪一版」这一件事，**不动任何任务与产物**——不重跑、不花钱。
  - 点「切到最新」回到跟最新走。
- **版本从旧往新编号**（第 1 版是最早那次生成）：用户嘴里说的「第 2 版」就是这个号，倒过来数会让人对不上。
- **只列成功的那几条是不够的**：某一版可能一条都没成，而用户最想知道的恰恰是「这一版为什么不能用」，所以失败条数一并列出。

### 说明（边界写清楚，别过度承诺）

- **一版 = 一次运行派出的那批任务**（也就是已有的批次标记）。**不新开一张表**——那个标记本来就是版本，只是以前没人把它当回事说出来。老数据里没有这个标记的任务，每个自成一版（那正是它们本来的样子，回填反而会编造出「哪些任务属于同一次运行」这个我们并不知道的事实）。
- **回滚是显式的，生成新产物不会推翻它**：回滚到第 2 版、之后又生成了第 4 版，节点交付的仍然是第 2 版——「用户刚说过要用哪一版」这件事不该被一次生成悄悄改掉。节点上会把「当前交付的是第 N 版（不是最新）」标出来，并给一键切回。
- **整图运行会忽略回滚**，并在确认弹窗里说明白：整图会把每个可执行节点都重跑一遍、它们各自都会产出新版，这时还按旧版给下游，用户会看到「我刚跑的图没被用上」——那比「回滚没生效」更难解释。**想让回滚生效就只单跑下游那个节点**（这一条写在弹窗里）。
- **回滚到的那一版如果已经不在了**（历史任务被清理过），退回最新那版，并如实说一句，不装作没事。
- 「重新生成」出来的任务**留在原来那一版**（它就是补上同一版里失败的那条），单任务节点也一样——这一版给所有节点类型都盖上了批次标记，不然单任务的节点重跑一次就自成一版。
- 无数据库迁移；新增接口 `GET /api/canvas/{id}/nodes/{node_id}/versions`（**只读**：回滚走的是普通的保存画布那条路，不另开一个写接口，免得出现两条写画布的路径）。
- **顺手把一处重复收口了**：「这个节点交付哪一批产物」原来在 `_latest_task_assets` 与画布 `/status` 里各写了一遍，现在共用 `group_versions` / `pick_version`——不共用的话，迟早出现「列表说 3 版、画布显示的是新版、下游读的是旧版」。
- 后端单测 **724 项全绿（49 个文件）**：新增 `test_canvas_versions.py` 25 项。`tsc --noEmit` 通过。
- **真跑验证（一分钱不花，20 项）**：造两版真 PNG 当上游、视频走本机假方舟，验证「回滚之后下游到底拿到哪一版」——没回滚时任务用第 2 版的图；回滚后同一条路用**第 1 版的两张图**（首帧 a1、尾帧 a2，整批按顺序都用上了）。这是这一项唯一真正要证的断言，而它穿过了「画布文档 → /status → 单节点运行 → 上游取值 → 任务参数」五道，任何一道没接上，界面上看起来都完全正常。

### 修复（写测试与浏览器走查时逮到的）

- **面板「选了半天没反应」**：浮框里显示的是**接口算出来的**当前版本，而用户刚改完、画布还没保存时接口回的仍是上一次保存的结果。改成以节点上的字段为准，选完立刻就对。
- **产物网格停在旧那一版**：网格与画布角标是后端按**已保存的**画布算的，切完版本要等这次自动保存落库再补刷一次节点状态——否则会出现「标签已经是第 1 版、缩略图还是第 2 版」。两处各补了一条测试守着。

## v1.1.21 · 2026-09-19

批次 8 第三项：**逐镜对白**——把「一张图 + 一条配音」补成「每位角色一个音色，逐镜台词自动配音」。上一版能挂一条配音是**手工挑**的；这一版把它交给分镜表。

### 新增

- **画布视频节点勾「逐镜对白（数字人）」**，再填两样东西：**默认音色**（留空用服务的默认）与**角色音色表**（每行一条 `小焰=nova`）。
  - 音色表认 `=`、`：`、`:` 三种分隔符，空行与 `#` 注释行跳过。**中文冒号必须认**：国内用户十有八九会打「小焰：nova」，而认不出来时的表现是「静默用了默认音色」，用户只会觉得这个功能坏了。
  - **认不出的行不静默丢掉**，而是在花钱之前报错并把那一行原样回显（`「小焰 nova」` 少了等号，就直说少了等号）。这一条错会跟着**一次付费的配音**一起发生。
- **每一镜的台词各自合成一条配音，再附给这一镜的视频**，画面里的人就说这句话。
  - **先合成、再判时长，最后才派视频任务**。顺序不能反：「这一镜的台词读不读得完」只有拿到配音时长才知道，而**配音便宜、视频贵**。
  - **说话人取自台词前的标签**（分镜表里写「小焰：你终于来了」就认「小焰」），与「剥掉标签不念名字」**共用同一个正则**——各写一个迟早漂移，漂移的表现是「标签剥掉了、说话人没认出来」，于是配音悄悄用了默认音色。
  - **认不出说话人不猜**：用默认音色，不替用户编一个角色出来（编错了就是别人嗓子在说话）。
- **读不完的那一镜只跳过这一镜**，视频照样出——并在日志里逐条说明「镜头2 的台词要 9 秒、这一镜只有 2 秒」，同时给出两条能照做的路（把台词改短 / 把分镜表里这一镜的时长写长）。建议指向的是**用户真能改的那个地方**（逐镜的时长来自分镜表，不是节点上那个兜底时长）。
- **一镜都配不上时，一条视频任务都不派**，并说明为什么。派出去就是拿视频钱换一串没声音的片子。
- **一句台词都没有 / 角色音色表有坏行 / 语音模型没配好**：三种情况都在**提交之前**报错，一个字的钱都不花。预览也会走到这几步，所以确认弹窗里就能提前看到「这一跑必挂」。
- **预览绝不花钱，但把额外那几次调用单独说出来**：勾了逐镜对白时，确认弹窗会多一行「逐镜对白：这一跑还会额外做 N 次语音合成（按所选语音服务计费，**不计在上面那个次数里**）」。它不占任务名额——不说的话，用户看着「这一跑 3 次调用」的账，实际还会再花 3 次配音的钱。

### 说明（边界写清楚，别过度承诺）

- **一镜一条音轨**：一镜里换好几个人说话是另一件事（那要按句切分、每句一条音轨），不在这一版。说话人取**第一条**可念台词上的标签。
- 节点面板按注册表的 `features` 画（新增 `shotDialogue`）——声明了却不读，那份声明就是摆设。
- 无数据库迁移；`services/speech.py` 新增 `speaker_of()` / `dialogue_parts()`（并与整段旁白共用 `_speakable_lines()`，两个入口对「哪些行算台词」只有一套说法）；`services/digital_human.py` 新增 `parse_voice_map()` / `voice_for()` / `skip_reason()` / `dialogue_summary()`。
- 后端单测 **696 项全绿（48 个文件）**：`test_digital_human.py` 38 项、`test_speech.py` 40 项、`test_frontend_digital_human.py` 10 项。`tsc --noEmit` 通过。
- **真跑验证（不花一分钱，42 项）**：假 TTS **把每次收到的请求落盘**，于是能证明「不同说话人真的用了不同音色」——小焰 → `nova`、阿澈 → `echo`、第三镜的小焰 → `nova`，且念出来的文本里不带「小焰：」标签；3 次配音 → 只派 **2 段**视频（读不完的镜头2 没派）；两条任务各自挂**自己那一镜**的配音，两次发出去的 `data:audio/wav;base64,…` **不一样**（没串线）、解出来都是真 WAV；日志里逐条给出少那一镜的原因。浏览器实测：面板三项都在、确认弹窗出现「额外做 3 次语音合成」。

## v1.1.20 · 2026-09-19

批次 8 第二项：**数字人（口播 / 对白镜头）**——一张图 + 一条配音，出一段会说话的视频。

**动手前先实测了 provider 能力**（这是路线图里定下的规矩，不能凭猜做）。结论决定了这一版的形状：

- **能用的**：火山方舟 Seedance 的 `content[]` 支持 `audio_url` + `role=reference_audio`（**Seedance 1.5 pro / 2.0 / 2.5 认，1.0 系列不吃**），且音频可以是 base64 data URI——本机文件直接塞进请求体就行。
- **排除的**：专用的对口型接口（火山 AI MediaKit 的 `lip-sync`、阿里百炼的 `wan2.2-s2v`）**都要求图片与音频是公网可访问的 URL**。我们是本机工作台，产物在自己硬盘上，为了这个功能去接一个公网图床，等于把「本机可离线用」换成「必须联网才能用」——所以排除，不是懒。

### 新增

- **对白音轨（数字人 / 口播）**：视频生成页与画布的视频节点都能挂一条配音，出片自带声音、画面里的人就在说这句话。
  - 音频从资产库的**音频**里挑（`kind=audio`），只列配音，不混图片视频——混在一起用户总会挑错一次，而这一次错要花视频钱才发现。
  - 选择器把配音的**秒数**一并显示：视频时长够不够读完整句，全看这个数字。
  - **时长不够时拦住，不自动改时长**：配音比所选时长长就报错，并给两条可照做的路（「把时长调到 N 秒以上」/「把这段台词拆到下一镜」）。自动把 5 秒改成 10 秒看着贴心，实际是**在用户没点头的情况下多花钱**，而这一笔花了多少恰恰最不该含糊。
  - **掏钱前那一次确认多了一行「对白」**：它决定买到的是有声还是无声的片子，必须再摆一次。
  - 任务日志里留一行「对白音轨：…（N 秒）」：视频时长是按配音长度定的，事后想弄清「为什么这次是 10 秒」得靠它。
- **本地先拦两道**（省一次必然失败的上游往返）：格式不是 wav / mp3、单个超过 15 MB。
- **上游拒绝时说清怎么办**：带音频的 400 会把「只有 Seedance 1.5 pro / 2.0 / 2.5 认参考音频」这句补上去——不然用户只看到一句「上游拒绝了这次请求」，而这一句其实是有解的。这句话**只有一份**（后端 `AUDIO_REF_NO_HINT`），两个前端入口都从接口拿，不各写一份。
- 实现不了就报错，**绝不静默丢掉音频**：百炼那条路会直说「数字人是另一个接口，且要求公网 URL，本机满足不了」；音频被删了 / 选错了资产 / 文件不在了，任务如实标失败并说明原因（**这些都在提交之前失败，不会扣钱**）。

### 说明（边界写清楚，别过度承诺）

- **`reference_audio` 是「参考」，不是嘴型驱动**。官方参数表里没有 `lip_sync` 这一类开关，所以它给的是「这个人用这个音色说这句话、口型看着对得上」，**不保证逐字对齐**。界面文案按这个口径写。
- **参考音频认模型**：Seedance 1.0 系列会直接 400。模型名单只用来写提示，**不做闸门**——新模型一直在出，拿名单拦人必然拦错。
- 无数据库迁移；新增 `services/digital_human.py`（时长口径与文案的纯逻辑）。
- 后端单测 **680 项全绿（48 个文件）**：新增 `test_digital_human.py` 26 项（纯口径 / 方舟请求体形状 / 接线 / 接口）、`test_frontend_digital_human.py` 7 项。`tsc --noEmit` 通过。
- **真跑验证（不花一分钱）**：本机起一个假方舟服务，把收到的请求体原样落盘，于是能逐字看「到底发了什么」——`content` 顺序、`role=reference_audio`、`data:audio/wav;base64,…`（解出来是真的 WAV）、model 名、时长没被偷改；不挂对白时请求体里一个 audio 字段都没有；换一条更长的配音 → 400 拦住且**一个任务都不落**。浏览器实测两个入口：选择器列出配音与秒数、确认弹窗显示「对白」、任务跑完有产物；画布视频节点同样有这一栏。

## v1.1.19 · 2026-09-19

批次 8 第一项：**配音（TTS 试听 + 样片旁白）**——写下几句话，选个音色，点一下就听到；样片从此不只有画面。

**为什么先做它**：数字人（#39）的三块里，形象（角色设定图）已经有了，缺的是**声音**与**驱动**。声音这块不落地，「每位角色一个音色」就无从谈起；而它自己也是独立有用的——分镜表里写好了台词，人的第一反应就是「念出来听听」。

### 新增

- **「配音」页**（导航新增，排在「视频生成」之后）：写下要念的内容 → 选音色 → 生成并试听。
  - **试听就是生成，产物直接进资产库**。这一段是花过钱的，必须能重听、能复用（样片旁白、以后的按角色配音都用它）；做成「听一次就没了」等于每次重听都要再付一次。
  - **音色是「快捷选项 + 可手填」**，不是下拉白名单：各家音色名不通用（`alloy` / `zh-CN-XiaoxiaoNeural` / 自建音色 id），写死选择框等于逼用户等我们发版。留空是**合法值**（用服务默认音色），所以留空时不会往服务商带一个空字段；默认语速同理不带。
  - **段落上限 2000 字**：上限由后端给出，界面照着显示与禁用，超了提示分段——一次性塞几千字进 TTS，失败时的错误信息也很难看。
  - 生成过的几条列在右侧，方便来回对比音色：**共用一个 `<audio>` 元素换 src**，连点几条不会叠着放。
- **样片旁白**（接在 #29 的静图样片上）：分镜图节点面板勾「加旁白」→ 旁白念分镜表里每镜的「台词」，整段一条音轨合进成片。
  - **只念台词，不拿画面描述凑数**。画面描述是给生图模型的提示词（「中景，角色站在花园中央，缓缓抬头」），念出来是制作说明而不是旁白；一句台词都没有时直接报错并指路（「先去分镜节点补一行『台词：…』」）——悄悄拿画面描述顶上，用户会听到一版莫名其妙的解说词。
  - **先做旁白再渲染**：语音合成是一次付费调用，配置不对、台词为空这些情况要在几分钟的渲染之前就报出来。
  - **整段一条、不与镜头逐一对齐**（逐句对齐留给按角色配音那一步），并如实说明「旁白比画面长多少秒、末尾会被截掉」。开了旁白的样片不再写「零成本」——那句话决定用户点不点，不能含糊。
  - **写成「（无）」的镜头会被跳过**：分镜表里没台词的镜头通常写成 `（无）` / `无` / `—`，照念会念出一串「无无无」。
- **音频资产在资产库里有自己的样子**：类型筛选加了「音频」，缩略图是「波形 + 时长」（直接塞 `<audio controls>` 会把卡片撑变形），点开是灯箱里的音频试听。
- 新增配置项**默认语音模型**（系统设置 → 默认值）：留空时，**只有一个语音模型就自动用它**；有多个则要求先选定——猜错一次就是替用户花一次钱。

### 修复（写测试与真跑时逮到的）

- **旁白比画面短时，成片会被截短**。封音轨用了 `-shortest`，本意是「旁白比画面长时末尾截掉」，但它的语义是「输出以先结束的那条流为准」——配一段 4 秒的旁白，一条 9 秒的样片就只剩 4 秒画面。现在补静音（`-af apad`）把音轨垫到画面结束，画面长度不再受旁白长短影响。这条是**真跑 ffmpeg** 的用例逮到的（断言成片时长），只测逻辑看不出来。
- **`application/octet-stream` 会存成 `.bin`**。`/media/` 是按扩展名猜 MIME 的，存成 `.bin` 之后浏览器拿到 `application/octet-stream`，`<audio>` 直接不认、进度条也拖不动，而后端一声不吭。改成**只认 `audio/*` 这一族**，其余一律按 mp3 存——与其一个个列「哪些不算」，不如反过来只认「哪些算」。
- **「小焰：」这类说话人标签没被剥掉**（当初的注释声称支持 `【小焰】`，正则其实匹配不到）。现在两种写法都剥，且只剥 ≤12 字的短标签，不会把正文里的长句误伤。
- **旁白拼接会多补一个句号**：「……最后一班车还没来。」后面再补一个「。」，念出来是一段多余的停顿。现在只在上一条没有句末标点时才补。

### 说明

- **为什么配音是同步接口**（不像出图出视频那样走任务队列）：这个功能的用法就是「写几句话 → 点一下 → 立刻听到」，走队列要多插一次轮询（默认 3 秒一轮），正好把「试听」的意义弄没了。代价是没有任务中心那一行，所以配了两条补偿：产物落成资产（能重听能复用）、文本留在页面上（点第二次就是重试）。
- 有数据库迁移（`0015_audio_modality`）：打开老库里那一行预留的「音频」能力，并按新默认值重排导航。**只动仍是旧默认值的行**——用户自己调过顺序的一律不碰。`ensure_seed` 只插缺行、从不覆盖，所以这两件事只能写迁移。
- 配音走 OpenAI 兼容的 `/audio/speech`（兼容面最广）。方舟、百炼、ComfyUI 的语音各是另一套口径，这一版都不硬做，但**报错会说清原因与接法**（方舟/百炼请用「OpenAI 兼容」类型接入自己的兼容地址）。
- 后端单测 **648 项全绿（46 个文件）**：新增 `test_speech.py` 37 项（纯逻辑 / 适配器契约 / 接线 / 真跑 ffmpeg 四层）、`test_frontend_speech.py` 13 项。`tsc --noEmit` 通过。
- 验证方式：本机起一个**假 TTS 服务**（返回真的 WAV 正弦波，时长按字数算），既走真 httpx、真落文件、真被 ffprobe 量时长、真被 ffmpeg 封进 mp4，又**不花任何外部额度**。浏览器实测：配音页生成与试听（`<audio>` `readyState=4`、时长 4.62 秒）→ 资产库「音频」筛选与灯箱试听 → 样片旁白出片（成片 9 秒、`webkitAudioDecodedByteCount > 0`、报告「旁白：2 句 · 21 字 · 4.6s（音色 alloy）」）→ 台词全删时如实报 400 并指路。

## v1.1.18 · 2026-09-18

批次 7 第四项：**运行中巡检**——跑着的时候，把「已经没人推进」的任务救回来或如实收口。

**为什么要有它**：视频与工作流的推进挂在一个后台轮询协程上。那个协程一旦因为意外（上游抽风、网络抖动、数据库忙）静默结束，**任务行会永远停在 processing**：进度不动、按钮灰着，用户只能重启服务才走得出来。启动恢复只在重启时兜一次，跑着的时候没有任何人管。

### 新增

- **运行巡检**：定时扫一遍未完成的任务（默认每 60 秒），把判为「卡住」的处理掉。判据是**两条都要满足**：
  - **进程内已经没有人在推进它**——快慢不是判据：一个大模型出图十分钟、一个视频等半小时都是正常的，轮询协程只要还在写进度就说明它活着；
  - **超过「任务无进展判定」（默认 10 分钟）没有任何写入**——只差第二条会把刚建好还没轮到投递的任务误杀（那只是几百毫秒的窗口）。
- **两种收口方式**，取决于还有没有可能接回去：
  - 有上游任务 id 的视频 / 工作流任务 → **重连**（把轮询重新挂上，上游还在跑就不浪费这一笔），同一任务最多 3 次，过了就如实收口；
  - 其余（图片 / 文本，或连上游 id 都没有）→ **收口成失败**，话说清楚是谁做的、下一步怎么办：「这条任务长时间没有任何进展，后台推进已经中断（运行巡检发现），已替你收口。直接重试即可。」
- **留下的痕迹在任务自己身上**：收口与重连都会往这条任务的日志里补一行（任务中心 → 日志），任务行上的报错也是同一句话。用户回头翻的时候能看出「进度停过、是谁接的」。
- 两个可配项（系统设置 → 系统配置 → 限制）：**任务无进展判定（分钟）** 与 **运行巡检间隔（秒）**。写成 0 或负数不是「更激进」而是没意义，会退回默认值——夹到下限等于让一次手滑变成「1 分钟就动手」，把正常任务扫进来。

### 修复（同一个洞的另一半）

- **重连那条路的轮询没有兜异常**：`_run_video` 里的轮询包了 try/except，但**重连（启动恢复）这条路以前没有**——协程里未捕获的异常只换来 asyncio 一句 "Task exception was never retrieved"，任务行永远停在 processing。也就是说，一次网络抖动就能换来一条只能靠重启才出得来的任务。现在重连统一走带兜底的轮询入口，炸了就把任务如实标失败（上游的错误原文照写）。

### 说明

- 无需数据库迁移；不改前端（痕迹落在任务中心已有的「报错 + 日志」上）。
- 巡检自己出问题不会影响应用：某一轮失败只记日志，下一轮照常；应用退出时会先把巡检停掉再收摊。
- 判断逻辑集中在 `backend/app/services/patrol.py`（纯函数，含「不许误杀」的判据），执行器只负责「按判断动手」。
- 后端单测 **598 项全绿（44 个文件）**：新增 `test_patrol.py` 29 项（纯判据 11 / 接线 18，含真造一条卡住的任务看它被收口、看「刚建的任务一根手指都不许碰」、看重连上限）。
- 真跑验证（真实库，临时把巡检调成 15 秒 / 判定 1 分钟）：卡住的图片任务被收口（任务日志里留下「运行巡检发现…已替你收口」）、刚建的任务保持 processing 没被碰、有上游 id 的视频任务被重连（日志「重新挂上轮询（第 1 次）」），重连起来的轮询随后因假服务不支持视频能力而失败——**也如实标成了失败，而不是又留一条卡死的**。

## v1.1.17 · 2026-09-18

批次 7 第三项：**预算闸与运行对账**——「运行整图」是「一次点击派出几十次调用」的入口，现在跑之前先拦一下、跑完之后对一次账。

### 新增

- **整图运行调用上限**（系统设置 → 安全 → `safety.run_call_budget`，0 = 不限制）：`运行整图` 的预估调用次数超过上限时，确认弹窗要求**再确认一次**——是一个勾选框、点它之前「开始运行」是锁着的（点两次同一个按钮不叫确认）；接口侧也会拒绝没有确认过的请求（409），绕过界面直接调也没用。
  - **只在「已经确定超限」时才要求确认**：还有节点的次数要等上游跑完才知道时只提示、不拦截，否则一个还没跑过的项目会永远点不动「运行整图」。
  - **闸门在服务端重算**，不认前端传来的数字：预览之后用户可能又改了画布，拿过期的确认值放行等于没设闸。
  - 单节点运行不受此限——它本身就是省钱的那条路。
- **跑完弹出「这一跑的实际账」**：实际调用几次、成功几条、失败几条、出了多少产物、耗时多久，外加逐节点的次数与失败数；并和受理时算的预估**摆在一起比**（差 3 次以上、或者有失败，就转成警示态）。
  - **对账的口径是「我们自己发出去的调用」**（一个任务一次），不是供应商回的 token：发了几次是可数的、也是我们要为之后果负责的那部分；上游报不报用量由它决定，报了我们也不用（没有价格表，换不成钱）。弹窗里明写这一句，免得被当成供应商账单。
  - **失败也照样算调用**：弹窗会直说「失败的任务不会因为失败就不计费」，并指向任务中心看那几条的报错。
- **`tasks.run_id`（迁移 `0014`）**：把「一次运行」派出的任务串起来，事后才能算出「这一跑实际派了几次」。单节点运行与整图运行各有一个自己的 id。

### 修复（浏览器真跑时逮到的）

- **「跑完了」的判断曾经是错的**，会让账比实际少一截。整图是**边跑边派**任务的（第二个节点要等第一个跑完才知道派几条），而 `finished` 当时只看「**已有**的任务都结束了」。于是两个出图节点的项目，第一个跑完就弹出「实际 1 次（预估 2 次）」，前端也随之停止轮询——**拿半截数字当结论，比不显示更糟**，而这恰恰是这个功能最不该出的错。
  - 现在给每一跑在内存里记一份「还在进行中」（`_ACTIVE_RUNS`）：`finished` 要求「已有任务都结束」**且**「整图本身走完了」。整图执行拆成 `_run_full_graph`（`try/finally` 负责收尾）与 `_walk_graph`（真正走图），中途炸了也会把这一跑标成结束，不会让前端一直等下去。
  - 受理时算出的预估随这一跑记下来（`expected`），刷新页面后仍能和实际摆在一起比。

### 说明

- 有数据库迁移（`0014_task_run_id`，新增可空列 + 索引），启动时自动执行。
- **没有采集 token 与费用**：那要动四个适配器、而且没有价格表（各家计费口径不同，换不成钱）。这一版只记「我们自己发出去的账」，够回答「这一跑打出去多少」。
- 后端单测 **569 项全绿（43 个文件）**：新增 `test_run_budget.py` 23 项（含「边跑边派时不能报跑完」「中途炸了也要收尾」「run 记录不无限增长」）、`test_frontend_budget.py` 10 项。`tsc --noEmit` 通过。
- 浏览器实测：上限设 1、画布会派 2 次 → 弹窗出现勾选框且「开始运行」锁住、勾选后才解锁；不带确认值请求接口 409；跑完弹账「实际 2 次（预估 2 次），和预估一致。成功 0 条，失败 2 条，耗时 10 秒」，与服务端 `run-summary` 逐字一致。

## v1.1.16 · 2026-09-18

批次 7 第二项：**静图缓动样片**——分镜图出完之后，先花零成本看一版带运镜的片子，再决定要不要掏视频钱。

### 新增

- **「分镜图」节点新增「静图样片」区块**：把这一节点已经出好的分镜图，按上游分镜表写的**时长**与**运镜**串成一条片子。本地 FFmpeg 渲染，**不调模型、不建任务、不花钱**。
  - **运镜照分镜表来，不是统一套一个动效**：推近 / 拉远 / 左摇 / 右摇 / 上摇 / 下摇 / 环绕 / 固定，各按分镜表里写的走。分镜表写「固定」就真的不动——那样才能看出「这场戏全是固定镜头，是不是太闷」；给所有镜头套同一个推近，等于把运镜栏当废纸。
  - 同一栏里写了多个运镜时（「横移跟拍」「推进跟拍」都含「跟」），**以先出现的那个为准**：写分镜的人总是先说主要的那个。
  - **只有需要挪视窗的运镜才超采样**：推/拉只是改缩放，源图按输出尺寸取（画面是像素级的）；摇/移/升降要四周有余量，才放大 1.25 倍再挪。静止与推拉镜头不会被无谓地重采样一遍。
  - 四个设置：**画幅**（跟随图片 / 16:9 / 9:16 / 1:1 / 4:3；跟随图片不裁切，选了固定比例才按比例裁切填满）、**清晰度**（720p / 1080p）、**兜底时长**（分镜表没写「几秒」时每镜按几秒算）、**没写运镜时**（轻微推近 / 固定不动）。
  - 产物直接进资产库（可在导演台里当素材用），也能在面板里播放、下载；**地址记在节点上**，刷新页面后那条片子还在，不用为了再看一眼重新渲染。
- **少了哪几镜如实说**：还没出图的镜头、因为总时长上限（120 秒）被截掉的镜头，都列在面板上，并写进产物说明里。样片少了一镜却不说，用户会拿一条缺镜的片子去判断节奏。
- **单镜只缩不放**：跟随图片时成片长边不会超过原图长边，避免把 512px 的图糊成 1920。

### 修复（当天在真实环境里踩出来的）

- **「库里有一行资产」不等于「文件还在」**：数据目录被清理过之后，`Asset` 行还在、图片 404，这种图交给 ffmpeg 只会得到一句「Invalid data found」。现在按「这一镜跳过」处理，并在报告里写明「图片文件不在了」；全都不在了才报错，且直接说「可以先重跑一次分镜图节点」。

### 说明

- 无需数据库迁移，不动生成链：它不是一个画布节点、不进自动链、不产生任务。
- 出片要走本地 CPU，所以**同一时间只允许渲染一条**（正在渲染时接口回 409，而不是让人默默排队两分钟）。
- 后端单测 **536 项全绿（41 个文件）**：新增 `test_animatic.py` 25 项（含真跑一遍 ffmpeg 并用「手算出来的取景框」当标准答案逐像素比方向）、`test_frontend_animatic.py` 9 项。
- 语音（配音 / BGM）随 #26 TTS 一起做，这一版是**无声**样片。

## v1.1.15 · 2026-09-18

批次 7 第一项：**分镜体检**——在上游把镜头语言查一遍，别等烧完钱才发现「六个镜头全是中景」。

### 新增

- **画布工具栏新增「体检」按钮**：零成本静态检查一份分镜表的镜头语言。**不调模型、不建任务、不写库**，随手点都不心疼。
  - 检查项：景别单调 / 一家独大 / 跨度太窄，运镜连续雷同 / 一家独大 / 种类太少，相邻镜头重复描述，AI 腔套话密度过高，首帧太短，只有一个场景却排了很多镜，字段缺失（画面 / 首帧提示词 / 时长）。
  - 逐节点给出**景别与运镜分布、总时长、命中条数**——一眼就能看出「六个镜头全是中景」不是风格统一，而是没分镜。
  - 每条提醒都说清「为什么不对」和「怎么改」，措辞落到可执行的动作上（「仿佛在诉说」→「手指在桌沿敲了两下」）。
  - **只告警不阻断**：报告形状与生成前的预检一致（`blocking` 恒 `false`），前端复用同一个告警组件。
- **跳过与失败都如实说**：还没跑过的分镜节点会列进「跳过」并说明原因（不是当成「没问题」）；格式写歪的分镜表会明确报「解析不出镜头表，下游的逐镜出图/出片会直接失败」，并给出正确写法。非分镜的文档节点（小说/剧本）解析不出镜头表是正常的，静默跳过，不刷屏。
- **与生成共用同一个解析器**：体检调用的是生图/生视频用的 `storyboard_sheet.parse_storyboard`，所以「体检说有几镜」和「生成时有几镜」永远一致。

### 修复（当天在真实项目上实测发现）

- **把节点的「生成要求」当成了内容，误报「解析不出镜头表」。** 体检原本会兜底读节点的 `prompt`；但对文档节点来说 `prompt` 是指令（例如「请按三幕结构改写，每镜不超过 4 秒」），不是内容。结果一个**还没跑过**的分镜节点，只因为输入框里写着 126 字的生成要求，就被判「下游的逐镜出图/出片会直接失败」。现在内容只看两处：手改正文（`docText`）与已生成的正文。`test_canvas_lint.py` 里留了反向用例守着。
- **「没查出问题」这句话永远显示不出来。** 它原本塞在 `PreflightNotice` 的 `headline` 里，而那个组件在告警为空时直接返回 `null`——干净的分镜表点完体检，界面上什么都不显示，用户分不清「没问题」和「按钮没生效」。现在干净时渲染一个独立的绿色确认块，并限定「至少体检到了 1 个节点」，节点全被跳过时不会误报「没问题」。

### 说明

- 无需数据库迁移，无破坏性改动。
- 后端单测 **502 项全绿（39 个文件）**：新增 `test_storyboard_lint.py` 17 项（纯规则，含反向用例与「每条规则都被本轮用例打到」的元测试）、`test_canvas_lint.py` 10 项（接线：内容来源、跳过与报错的分界、只读、JSON 键名契约）、`test_frontend_lint.py` 7 项（先存再查 / 干净要说 / 跳过要列）。

## v1.1.14 · 2026-09-18

小版本，修一处「文档承诺了、代码没兑现」的自我不一致，没有新功能。

### 修复

- **3D 导演台：机位「跟随目标」的字段与校验补齐。** `types.ts` 的注释里写着「跟随目标只保留字段」，但那个字段其实没加。现在补上了 `trackTargetId`，并把校验做对：
  - 指向存在的角色/元素 → 原样保留（导入别的实现导出的场景时这层关系不丢）；
  - 指向不存在的对象 → 清成 `null`；
  - 指向另一个机位 → 也清成 `null`（只能跟角色/元素）。
  悬空引用留在存档里不会报错，只会在渲染时静默失效——这类问题最难查，所以在读入口一次清干净。

- **3D 导演台模块头部那句「没做的部分」清单改正并写全。** 原注释只列了三项，实际是六项，且漏掉了最要紧的一项（架构差异，不是缺按钮）：参考实现里 3D 场景是**挂在画布节点上、随项目走**的，本项目是「导演台页第二个页签 + 本机存档」，场景不进项目文件。其余四项：本地上传 GLB/GLTF 模型（含 blob 只在当次会话有效的处理）、空对象占位、群众阵列（N×M 批量铺人）、对象分组界面。

### 说明

- 无需数据库迁移，无界面变化。
- `tests/test_director3d.py` 增加 3 条行为断言（跟随目标保留 / 悬空清空 / 指向机位清空），仍是 9 项测试，其中一项用 esbuild + node 真跑一遍校验器。

## v1.1.13 · 2026-09-18

这一版是批次 6 的第三项，也是 A–G 分期里的 **F 期：3D 导演台**。

「导演台」页面现在有两个页签：**视频粗剪**（原有功能，一行未改）与 **3D 预演**（新）。

### 新增

- **3D 预演（3D 导演台）**：在一个三维空间里把「人物站在哪、摄影机在哪、光从哪来」摆清楚，然后直接截图存进资产库——这张图可以直接当图生视频的首帧用。
  - **场景对象**三类：角色（素模，8 种体型）、元素（立方体/球体/圆柱/圆锥/平面/环体/棱锥）、机位（14 个影视级预设：正面中景、过肩、荷兰角、鸟瞰、低角度仰拍…）。
  - **角色姿态**：9 个预设（站立/T 型/坐姿/行走/奔跑/挥手/指向/蹲下）+ 18 个关节的滑杆（肩髋各 3 个自由度、肘膝各 1 个），拖滑杆的姿态改动整段只算一次撤销。
  - **视口**：导演视角（自由环绕）⇄ 机位视角（相机吸附到机位，所见即所得，支持荷兰角）；点选对象、**在地面上拖动**对象、锁定对象防误拖、隐藏对象。
  - **灯光**：三点布光（主光/辅光/轮廓光）可按强度与**色温**调，环境光与地面不透明度可调，四种环境预设（影棚/日景/夜景/室内）。
  - **出图**：7 种画幅（21:9 / 16:9 / 4:3 / 1:1 / 3:4 / 9:16 / 自适应）× 1–4 倍率，出图直接进资产库；也能把当前视角一键**存成机位**。
  - **撤销/重做**（Ctrl+Z / Ctrl+Y）、**导出/导入场景 JSON**、自动存到本机（场景按作用域存在浏览器里）。

### 说明

- **3D 模块是懒加载的**：three 约 1 MB，实测打成独立分块后主包只增加了 14 KB（gzip 从 174.5 KB 到 179.5 KB），不打开 3D 页签就不会下载它。
- **移动端/窄窗口**：窗口较窄时两侧面板自动收起，展开后是浮层而不是挤压画布，且两个面板互斥（同时开会把画面盖满）。
- 本版**刻意没做**（数据结构里留了字段，以后加不用迁移老数据）：导入 GLB/GLTF 模型、对象打组、跟随目标。
- 无需数据库迁移。
- 本轮修掉三个「看着像功能没做、其实是样式/尺寸」的问题，都记在 `MEMORY.md` 与测试里：画布没被 CSS 撑满导致画面被裁一块、窄窗口下顶部一排按钮被裁掉一部分（撤销点不到）、底部工具条被浮层面板压住点不到。
- 新增 `tests/test_director3d.py` 9 项：其中一项**真的把 `persist.ts` 用 esbuild 打成 ESM 让 node 跑一遍**，逐条验证场景存档的夹取/兜底/丢弃行为（版本不认整份丢、没 id 的对象只丢它、关节夹到滑杆范围、激活机位悬空要回落…），其余 8 项守接线（three 只能在懒加载模块里、画布要撑满、工具条层级要高于浮层、截图要把设备像素比压回 1、拖动只改 x/z…）。没有 node 时该项自动跳过并打印原因。

## v1.1.12 · 2026-09-17

这一版修一个用户报的现象：**拖动节点时，画布偶尔会自己乱窜**。

### 修复

- **拖动节点期间，画布不再自动平移。** 画布有个贴心设计：节点浮框超出可视区时自动把画布挪一点，让整块浮框都看得见。问题是它没有闸门，而触发它重新测量的时机里混进了「用户正在拖节点」：
  - 拖一个还没选中的节点时，节点会在拖起手的那一刻被选中、浮框随之挂载，并在挂载后 420 / 950 / 1500ms 各测量一次，这三次正好落在拖动过程中；
  - 有节点在运行时每 2.5 秒一次的状态轮询，也会让浮框重新测量。

  于是指针还按在节点上、画布先自己平移走了。现在指针按在节点上时一律不平移，同时把可能正在跑的平移动画也掐掉。浏览器实测（同一节点、同一浮框、溢出 51px 的同一状态）：

  | 操作 | 浮框是否溢出 | 画布视口 |
  |---|---|---|
  | 普通点一下选中 | 51px | 平移 50.76px 去对齐（功能正常） |
  | 按住拖，1.0 秒时（已过前两次测量） | 51px | 完全不动 |
  | 按住拖，2.0 秒时（已过第三次测量） | 66px | 完全不动 |
  | 松手之后 | 66px | 完全不动 |

- **浮框只在「自己的尺寸变了」时才重新对齐画布。** 浮框的位置会跟着节点和画布一起走，但只有尺寸变化才说明「内容真的不一样了」（产物出来了、状态条出现了）。只是位置变了而尺寸没变，说明移动的是节点或画布本身——都是用户自己拖的，这时候再把画布拽回去又是一次乱走。顺带也解决了「手动拖完画布，一有任务状态更新画布又被拽回去」。

### 说明

- 无需数据库迁移。
- 刻意**没有**做「松手后补一次对齐」。试过，松手那一刻会把刚放下的节点连同画布一起拽走（实测偏了 3105px，节点直接飞出屏幕），所以这条路径被测试明确禁止。
- `tests/test_frontend_canvas_nudge.py` 6 项守在这里：闸门必须在 `nudgeViewport` 自己身上且必须是 `useRef`、两个拖动回调必须成对设置/清除、松手回调里不许动视口、浮框的尺寸闸门必须写在平移调用之前。

## v1.1.11 · 2026-09-17

这一版是批次 6 的第二项：**首页的信息层级，以及创作页的第一步引导**。

### 改进

- **首页最主要的那个按钮，改成「按你现在处在哪一步」决定**，而不是固定成某个功能的入口：
  - 已经有作品 → 「继续「作品名」」——一点直达它的画布（老用户九成情况下想干的事）
  - 还没有作品、也还没接模型 → 「先接入一个模型」（否则点什么都只会撞到同一个错误）
  - 还没有作品但有模型 → 「从示例开始」
  判断依据全部来自已有接口（项目列表 / setup-status），不额外问你。
- **首页「最近项目」现在直接进画布**：以前点了只跳到项目列表页，想接着做的人还得多点一次——而组件本来就拿到了「打开画布」的回调，只是没用在这一处。
- **首页补上「还没有作品」的空状态**：以前没有项目时整块区域直接消失，页面缺一截、也没有任何引导。现在有一段说明和两个出口（铺一条示例 / 我自己建）。
- **创作页（图片 / 视频）的空状态给出三条示例提示词，点一下就填进输入框**：第一次进这两页，面对的是「左边一排参数、右边一片空白」，最容易卡在「画面描述该写什么」这一步。视频那三条都写清了「动作 + 运镜」——视频更贵更慢，最怕等了两分钟出来不是想要的。
- **首页卡片加入场动效**（轻微上移淡入、带阶梯延迟），并在系统开启「减少动态效果」时自动关闭。

### 说明

- 无需数据库迁移。
- 本轮动效只做了首页卡片入场一处：任务卡是异步新增的，给它们加动画会在每次轮询刷新时反复播放，看起来像闪一下，反而更差。

## v1.1.10 · 2026-09-17

这一版做的是批次 6 的第一项：**把弹窗的行为与可访问性统一**。

### 改进

- **弹窗行为统一**（`components/Dialog.tsx`）：以前同一个应用里有三套弹窗行为——任务详情那类 `Modal` 能按 Esc 关，画布里的 5 个自建弹窗与两个 Lightbox **关不掉**；`role="dialog"` / `aria-modal` 也只有 `Modal` 上有。现在全部收口到一处：
  - Esc 关闭、点遮罩关闭（只在点中遮罩本身时算，弹窗内部点击不会误关）
  - `role="dialog"` + `aria-modal` + 无障碍名称：读屏软件念得出跳出来的是哪个弹窗
  - 打开时把焦点移进弹窗，关闭时交还给打开前的那个元素——键盘用户不会在关掉弹窗后掉回页面顶部、再 Tab 一遍才能找回刚才的位置
  - Tab 在弹窗内循环，不会 tab 到背后看不见的页面上
- **皮肤与结构刻意没动**：`.modal` / `.canvas-dialog` / `.lightbox` 三套样式原样保留，弹窗内部一行都没改，只把外层的"遮罩 + 容器"换成统一外壳，所以外观零变化。

### 说明

- 无需数据库迁移。
- **顺手拆掉一个必然会发生一次的坑**：键盘监听原本依赖 `onClose`（每次渲染都是新函数），而画布页在弹窗打开期间还在轮询刷新——监听器被反复拆装，恰好落在缝里按下的 Esc 就会丢，表现成「偶尔按 Esc 没反应」。现在用 ref 取最新的回调，监听只绑一次。
- **关于「要不要把 `className="btn"` 都改成 `<Button>`」：本版不做。** 外观层（`.btn` / `.input` / `.card` / `.badge` / `.segmented` / `.spinner` / `.empty`）早就统一且完整，全项目 123 处按钮改成组件属于「改动面大、视觉零变化、收益最小」；真正缺的是行为统一，就是上面第一条。
- 遗留项：分段控件（`.segmented`，12 处）目前只用 CSS 类表达选中态，读屏软件读不出「当前选的是哪个」，也没做左右箭头切换。本次未动——它的键盘可用性本来就成立（都是原生 `<button>`，可 Tab、可回车）。

## v1.1.9 · 2026-09-17

v1.1.7 的更新日志里记过一条「已知问题」：导演台的「AI 改造」是坏的。这一版把它修好，顺带把同一处口径上另一个会让告警静默失效的漏洞补上。

### 修复

- **导演台「AI 改造」这条路径原本走不通**：导演台手上是**视频片段**，而视频生成接口的首帧必须是图片。以前只是把片段的 id 塞进草稿、直接跳到视频页，于是要等你填完参数、点下生成之后才被后端挡下（「首帧图片不存在或不是图片」）——反馈来得又晚，看着还像是模型的问题。现在会在跳转前先在本机截一帧（取这段的起点），把那一帧当首帧带过去；截帧失败会当场说清楚，不会带着空草稿跳过去。
- **截帧与截取片段的分辨率不再照抄来源**：这两处以前都是直接沿用源视频的 `width/height`。v1.1.7 之前入库的视频这两个字段一律为空，于是从它们截出来的图片也没有宽高——而图生视频的「首帧与画幅不符」「首帧会被放大」两条告警**靠读宽高判断**，读不到就静默跳过。现在截帧以**产出的那个文件**为准（纯 Python 读文件头）；截取片段在源没记尺寸时才去问一次产物，源已经有记录就不多跑这一步。截取片段的时长仍按你点的区间算（不取 ffprobe 的实际长度，省得「截了 10 秒」显示成 9 秒）。

### 说明

- 无需数据库迁移。
- v1.1.7 记的那条「已知问题」到此关闭。

## v1.1.8 · 2026-09-17

这一版是**批次 5「新能力」**：画布上多了个能听懂人话的搭图助手和一处分享出口，并把「这一跑要花多少」提前摆到你按按钮之前。

### 新增

- **AI 搭画布**（画布工具栏 → AI 搭画布）：一句话说明你想要什么，它给出**拓扑草稿**——用哪些节点、怎么连、每个节点要写什么。有几处是刻意这么做的：
  - **坐标不问模型**：模型只决定「选哪些节点、怎么连」，落位由后端按拓扑算（最长路径分列 + 同层纵向排布）。所以草稿一定整齐、不重叠、同一份输入得到同一份布局，而不会出现节点叠成一团或散到看不见的地方。
  - **只给草稿，不动画布**：草稿看完再决定要不要放到画布上；放上去还可以逐个改。挑模型是可选动作（不选就自动挑一个可用的文本模型）。
  - **模型给的东西不合法就修，而不是整份丢回去**：参数被改回默认、断环只丢那条多余的连线而不丢节点，改了什么都列出来告诉你。
- **结果状态是机器可读的，不再是一句「生成失败」**：缺模型凭据 / 需要你先改输入 / 模型没吐出可解析的结构 / 上游出错，四种情况分别给状态码与**下一步该做什么**，前端据此显示不同的话，不用去猜报错文本。
- **分享画布（零服务端）**（画布工具栏 → 分享）：把画布导出成一个带 `schemaVersion` / `requires` / `license` 的独立分享单元——可以给一段压缩过的**分享码**直接贴进聊天窗口，也可以存成 JSON 文件。授权范围四档（禁止商用 / 署名非商用 / 公有领域 / 保留所有权利），每档附一句人话解释。两条底线：
  - **不经过任何服务器**：导出与导入都是本地行为，没有上传、没有托管、没有分享列表。
  - **不替分享者的话背书**：导入时按**你本机**真实有的节点类型与模型重算一遍缺什么（缺模型只提醒、不拦），而不是相信对方清单里写了什么。
- **运行整图前先说清楚这一跑要花多少**（画布 → 运行整图）：点下去之前弹一屏预估——会执行几个节点、至少几次模型调用、哪些节点这次会一次扇出很多条、哪些节点已经有产物**会被重做一遍（再花一次钱）**、本次会复用哪些已有素材各用几次。其中几条是刻意这么定的：
  - **预估与真跑共用同一段代码**：预估走的是建任务逻辑的「只算不落库」模式（dry_run），所以弹窗上的数字不可能和实际派发的对不上；预览本身是纯读的，不建任务、不写库、不调模型。
  - **算不出来就说算不出来**：上游这次才会产出的节点（例如分镜表还没生成，自然不知道要出几张），条数标成「待定」并说明原因，而不是拍一个 1 或 0。
  - **已经能判定会挂的节点提前点名**：不必等整图跑到一半才断在那里。
  - 弹窗里保留出口（先不跑），并提示「只想补跑某一步就用那个节点上的单独运行」。

### 修复

- **逐镜出片的报错说不清**：视频节点若只接了「分镜图」而没接「分镜」，以前会报「节点没有可用的输入内容」——看着像是节点里的输入框没填，实际缺的是那条文本连线。现在直说「没能从上游内容里解析出分镜表：请把上游「分镜」节点接进来」。同一处口径也合并成了一份：「能不能跑」与「走哪条分支」以前各判各的，容易在改动里漂移。

### 说明

- 无需数据库迁移。
- 整图预估目前是「这一跑的形状」，不是金额：应用是自带 Key（BYOK）模式，拿不到各家的实时单价，所以给的是**调用次数**与**会被重做的节点**，具体花多少钱取决于你自己的服务商。

## v1.1.7 · 2026-09-17

这一版是「稳健与可维护」的一批：把几个**要等任务量上来才疼**的问题提前拆掉，并修掉两个一直在、但没人注意到的老问题（其一的表现是「时间早 8 小时」）。

### 新增

- **生成前会提醒一句，但绝不拦你**：提交前顺手检查一次「参数各自都合法、但组合起来很可能不是你要的」情形。有提醒就弹窗说明，点「仍然生成」照走，点「返回修改」回去改；预检本身失败（网络抖、后端版本旧）**不会影响提交**，最坏只是少提示一句。目前会提醒这些：
  - **首帧与画幅对不上**：首帧是 1280×720（16:9），这次却选了 9:16——出片会按 9:16 出，首帧多出来的那一边会被裁掉。这种事上游不报错，只会闷头裁切，等看到成片才发现白花了额度。
  - **首帧会被放大**：首帧短边只有 720px，却选了 1080p——上游只能放大，画面会糊，而糊掉就再也回不来。
  - **这次要排的任务超过队列剩下的名额**：直接告诉你「会排 5 个任务，现在只剩 3 个名额」，而不是等到看见一片失败任务才知道点多了。
  - **同一个模型最近连续失败**：把最近一次的真实错误一并带出来（例如「上游返回 503」），省掉一次白等。
  - **提示词太短**：只有一两个字时画面基本靠模型自由发挥，同一条提示词跑两次差别会很大。
  - 每条提醒都给出**怎么改**，而不只是「有问题」。
- **配置导入导出**（系统设置 → 配置导入导出）：把提示词库、导演风格、创作 Agent、服务商预设、参数档位等打包成一个 JSON 快照，方便换机器或分享。三条底线摆在明面上：
  - **不含 API Key**：模型服务只导出名称、协议、地址与模型清单；导入侧也拒收带 Key 的行（连密文都不碰）。
  - **本机参数不跟着走**：绝对路径、本机地址、本机服务编号会被排除，并且能展开看「排除了什么、为什么」。
  - **先预览再写库**：先给出「将新增 / 更新 / 跳过多少行、有没有冲突」，确认之后才写；写入的每一行都留可回滚的审计。

### 改进

- **队列满了会直说，而不是无限排队**：在册任务总数上限（默认 200，可在「系统设置 → 系统配置」里改「排队上限」）满了就直接拒绝并说明原因与调法；同一处也能调大「任务并发数」。
- **等上游出片的这段时间不再占用并发名额**：以前一个视频任务会**攥着名额等上游**，4 个视频任务就能把整个队列（包括图片）卡满 30 分钟——表现就是「点了没反应」。现在提交完立刻放名额，等待期只是定时问一下上游。
- **界面偏好统一合法化**：主题、侧栏折叠、画布档位、缩略图大小、记住的模型等全部收口到一处读写，读到非法值就地清掉并退回默认（留着它只会在每次进页面时重复失败一次）。其中「记住的模型」会**按当前实际存在的模型校验**——服务被删掉或改名之后，不会再留下一个空白的选择框。

### 修复

- **任务时间比真实时间早 8 小时**：库里 `created_at` 由 SQLite 写入（UTC），而 `completed_at` 由 Python 写入（本地时间），同一行两个时钟，实测差整整一个时区。后果不只是显示不准：任何「耗时多久」「最近 N 分钟有没有发生」的判断都会**安静地错**。现在写库只有一个时钟，出接口统一标 UTC，由浏览器换算成本地时间。任务中心、资产库、配置审计的时间随之恢复正常。
- **素材分辨率一直是空的**：`assets.width/height` 从来没有人写入过，所以画布里的素材选择器只显示文件大小、看不到「1280×720」。现在每条存图的路径都会顺手把宽高读出来（纯 Python 读文件头，不引入图像库）；库里没记的老素材会现读一次文件——少了这层兜底，首帧相关的提醒会对老素材静默失效。
- **上传素材的方块只能鼠标点**：换成真正的按钮，键盘也能选首帧与参考图。

### 说明

- 无需数据库迁移。
- 历史数据里已经写错的完成时间**不做批量改写**：那个字段目前没有任何界面在读，而「按一个猜测的时区偏移去改历史数据」有把数据改坏的风险。新写入的一律正确。
- 已知问题（**v1.1.9 已修**）：导演台的「AI 改造」把**视频片段**当作首帧传给视频生成页，而生成接口要求首帧必须是图片，这条路径会报「首帧图片不存在或不是图片」；修法是先截一帧再传。

## v1.1.6 · 2026-09-17

这一版是**拿真实 Key 试出来**的两处修补，外加把「保存失败」那句没头没尾的报错说清楚。

### 修复

- **火山方舟的 Key 认不出来**：方舟现在发的 Key 是 `ark-<uuid>-<尾码>`，而识别规则照的是老版的裸 UUID。把真 Key 粘进「粘贴一个 Key 就接入」，应用认不出归属，还会**拿这串 Key 去试 OpenAI / DeepSeek / Kimi 三家**（当然都是 401）。现在两种形状都认；已经在用的安装由迁移 `0013` 自动更新这条规则。
- **认不出归属时不再瞎试**：以前会按顺序挑三家试一遍——既白等五秒，更要紧的是**把用户的凭据发给了三家他并没有在用的公司**。现在认不出就直接把候选列出来让用户挑，一个请求都不发。
- **「保存失败：存在重复的唯一标识，请修改后重试」说不清是哪一项**：这是配置表（提示词库 / 参数选项 / 服务商预设 / 导演风格 / 创作 Agent / 系统配置）里最常见的保存失败。现在会明确说：「「标识」「xxx」已经被另一行占用了（提示词库里不能重复）。换一个标识再保存；如果想改的是已有那一行，请到列表里直接编辑它。」表单里也在「标识」下方标注了「同一张表里不能重复」。

### 说明

- 数据库多一条迁移（`0013`），启动时自动执行。

## v1.1.5 · 2026-09-17

这一版专治「下载之后到跑通之前」：第一次打开应用最容易卡住的几个点——没有模型、不知道该填什么、不知道能做什么——各自给一条立刻能走通的路；并给出 Windows 解压即用的便携包。

### 新增

- **首次进入的引导横幅**：还没有接入任何模型时，界面顶部直接说明「为什么现在什么都用不了」，并给出两条立刻可走的路（粘贴 Key / 接入本机 Ollama）。已经有服务、只是缺某一类模型（例如缺视频模型）时降级成提醒。关闭状态按「当前缺什么」记下来：将来情况变了，该提示的仍然会提示。
- **粘贴一个 Key 就接入**：「模型服务」页顶部贴一个 Key，应用按预设里的识别规则认出是哪家，用预设地址发一次**真实请求**，**通了才保存**。认不出来时不猜——把候选列出来让你挑（DeepSeek 与通义千问的 Key 形状完全相同，机器分不开是正常的），测不过也留了手动保存的出口。
- **检测并接入本机 Ollama**：探测 `127.0.0.1:11434`，把已经下载好的对话模型一键接进来，不需要任何云账号。向量模型与重排模型会被过滤掉——它们不能对话，混在下拉里只会让人选了之后报一个看不懂的错。
- **内置示例项目**：「从示例开始」会**按你现有的模型自动选档位**——只有文本模型就铺 L1 文本链（创意→小说→剧本→分镜），有图片模型才铺到资产链。第一个节点里写好了一句创意，打开就能点「运行整图」。
- **Windows 便携包**：`xiaoma-ai-studio-v1.1.5-win64-portable.zip`，解压后双击 `start.bat` 即可，**不需要先装 Python**（包内自带运行时与依赖，用户机器上不编译、不联网装包）。打包脚本是 `backend/tools/make_portable.py`。

### 改进

- **上游错误按状态码给出可照做的下一步**：401 提示核对 Key 与服务商是否配套、404 提示 Base URL 该填到哪一级、429 提示看余额与配额、5xx 提示稍后重试。网络类错误也分成了「连不上 / 超时 / 代理」三种说法，而不是笼统一句「网络请求失败」。
- 服务商预设多了「Key 识别规则」一列（可在「系统设置 → 服务商预设」里改），再加一家能从 Key 形状上认出来的服务商不必改代码。
- 接入完成后顶部横幅就地刷新（此前要切一次页面才会消失）。

### 修复

- **示例项目存下来了却打不开**：画布内部所有节点统一用 `contract` 这个渲染类型，而示例把内部名字原样存进了库，后端判「未知节点类型」——前端只看到一句报错，画布一片空白。现在画布与示例共用同一份「落库前整形」。
- **本机 Ollama 被误判成「没装」**：探测超时原本是 1.5 秒，而 Ollama 的 `/api/tags` 实测在 0.6–2.6 秒之间波动，于是**跑着的服务偶尔被判成不存在**——而「本机有 Ollama」正是第一次打开应用时最有用的一条信息。超时放宽到 4 秒（真没装时连接会立刻被拒，不会白等）。
- **只有 Ollama 时示例被挡在门外**：上一版示例固定铺 L2，需要图片模型，而「没有云 Key、只有本机 Ollama」恰恰是最典型的新手状态。

### 说明

- 数据库有一条迁移（`0012`，给服务商预设加 Key 识别规则），启动时自动执行。
- 便携包与源码包的区别、升级时怎么保留数据，见 `docs/portable.md`。
- 已知限制：像 OpenRouter 这类 `/models` 公开的服务商，连通测试无法校验 Key 是否有效（上游本来就不校验），这类 Key 的真正校验会发生在第一次生成时。

## v1.1.4 · 2026-09-17

这一版补上**自动链的最后一段**：分镜图之后能直接逐镜出视频，镜头之间接得上。顺带把任务中心里最常点的两个动作（重新生成、打开目录）做扎实。

### 新增

- **逐镜出视频**：video 节点多了「逐镜出视频」三档——`关闭`（老行为，整段出一个视频）、`逐镜各出一段`、`逐镜并接首尾帧`。开启后，**分镜表里有几个镜就派几个视频任务**，与分镜图节点同构。
- **每镜用自己那张分镜图当首帧**：逐镜配对**按镜号**而不是按顺序——上游正文被截断或补写过时，按顺序配会把别的镜头的图当首帧用出去。
- **同场景串联**（`逐镜并接首尾帧` 时生效）：某一镜的尾帧自动用**下一镜的首帧**，末镜不编造尾帧。另可开启「同场景参考串联」，把**同场景上一镜**接成参考，保证同一场戏里镜头之间接得上；跨场景不串（换场本来就该断开）。
- **逐镜时长**：分镜表里那一镜写了时长（如 `5s`）就用它——4 秒的镜头不该被拉成节点上设的 5 秒；没写才退回节点设置。
- **分镜表场景分组**：认 `## 场景2 | 黄昏的花园` 这类标题（`场景`/`幕`/`scene`/`act` 都认），标题没编号也能归组，不会散成一片。
- **任务「打开目录」**：任务卡片上有产物时多一个按钮，直接在文件管理器里定位到产物文件。
- **重新生成（原来的「重试」）**：不再让你回忆上次怎么配的——**沿用当时的参数快照**，只覆盖你在弹窗里显式改的项；按钮上会标「重跑自 #N」，任务中心能区分首次与重跑。重跑出来的任务**仍挂在原画布节点上**，产物会回到画布。
- **`--issue` 模式**：`python backend/tools/env_report.py --issue` 直接吐出**按 Issue 模板排好版**的内容，补三句话就能提交；「导出日志」弹窗里也多了「Issue 内容」页签。仓库里加了 `.github` / `.gitee` 的 Issue 模板。
- **`start.bat` 加 uv 兜底**：虚拟环境建不出来时（嵌入式 Python 没有 `venv` 模块）会再试 `uv venv --seed`，这是第三道回退。

### 修复

- **重复镜号会重复出片，费用翻倍**：上游正文有可能是多份分镜表拼起来的（重跑过上游节点，或手工粘了两份）。此前会按镜号重复派任务——3 镜变 9 段，**账单三倍，而任务数量上完全看不出异常**。现在分镜图与视频两侧都按镜号去重，并把去重数量写进日志。
- **短视频时长被解析错**：分镜表里写 `120s`（明显是笔误）会被读成 `12` 秒拿去出片。数字两侧加了边界，超出 1–30 秒范围一律视为未填。
- **「重新生成」早先只认图片通道**：视频、ComfyUI、文档类任务重跑会被当图片任务跑。现在按任务类型分发。
- **上游返回的错误正文可能把提示词带回来**：任务日志里现在只记结构化摘要（HTTP 状态、错误类型、响应体字节数）。

### 说明

- 数据库有一条迁移（`0011`，给任务加「重跑血缘」字段），启动时自动执行。
- 现有画布：video 节点默认仍是「关闭」，不改变已有作品的行为；想要逐镜出片需要在节点上手动开启。

## v1.1.3 · 2026-09-16

这一版做「出问题的时候，你能自己看清发生了什么，也能一键把该给的信息给出来」，同时把日志里的隐私收干净。

### 新增

- **日志落盘**：日志现在同时写一份到 `backend/data/logs/app.log`（单个文件上限 5 MB，保留 5 份，自动轮转）。此前只输出到控制台，窗口一关就什么都没有了。
- **导出诊断报告**：「系统设置 → 关于与更新 → 日志与诊断」里点「导出日志」，会生成一份报告——环境体检结论 + 最近 200 条错误与警告 + 日志文件清单。可以先在界面上完整看一遍，再「复制全文」或「下载 .txt」发给作者。也可以直接访问 `GET /api/logs/export.txt` 拿到同一份内容。
- **任务自己的日志**：任务中心的每张卡片多了「日志」按钮，能看到这个任务在执行期间产生的全部日志。多数问题看这里就够了，不必来问。

### 改进

- **日志写盘前先脱敏**：接口密钥、URL 查询参数、系统用户名与家目录路径会被替换成占位符。也就是说**日志文件本身就是可以安全外发的产物**，不存在「导出时忘了脱敏」这条路径。控制台保留完整信息，方便本机排查。
- **上游返回的正文不再进日志**：为了让用户看清原因，错误消息里会带上游返回的错误正文；但上游在 400 时经常把请求体回显回来，那里面就是用户的提示词。现在这类错误额外带一份枚举化的安全摘要（HTTP 状态、错误类型、响应体字节数），日志只记摘要、不记正文。这条在数据层做成强制的：拿不到安全摘要时日志会显式标注「已省略消息」，而不是退回去打印堆栈。

### 修复

- **日志一条都写不出去（静默失效）**：文件 handler 用了延迟打开，而重写的写入方法漏了补上文件句柄，结果每次写入都在内部报错并被吞掉，界面上完全看不出异常。
- **服务启动后的日志全部丢失**：数据库迁移（Alembic）会用自带的日志配置**替换掉根 logger 的全部 handler**，把刚装配好的控制台与文件 handler 一起冲掉。现在迁移不再接管已有配置，启动流程也会在迁移之后重新装配一次。
- **脱敏会把日志本身弄坏**：先洗格式串再插值，会把 `%s` 之类的占位符一起洗掉，于是插值时参数比占位符多，直接抛错、整条日志丢失。现在改成先插值再脱敏。
- 上游返回空响应体时，错误消息不再留下一个孤零零的冒号。

### 说明

- 日志目录在 `data/` 下，不会被「一键更新」影响。任务日志只保留在内存（最多 50 个任务 × 1000 行），服务重启后可在导出的报告里看完整记录。

## v1.1.2 · 2026-09-16

修「一键启动脚本卡在某一步、但看不出为什么」这个体验问题，并把安装说明补全。

### 修复

- **启动脚本对每一步都不检查成败，失败也继续往下跑**：有用户把项目放在 ComfyUI 的 `custom_nodes` 目录里，`start.bat` 的「安装后端依赖」这一步反复失败，而控制台只有一堆无从下手的红字。根因是当时的脚本不检查返回码——尤其是 `python -m venv` 失败时会**静默跳过**，紧接着 `call activate.bat` 找不到文件，pip 就装到了错误的解释器上，表现成「第二步一直失败」。
- 现在的启动脚本：每一步都检查返回码，失败立刻停下并打印可能的原因与下一步动作；不再依赖 `activate.bat` 切换解释器，而是**直接用虚拟环境里的 python 绝对路径**（少了「静默切不过去」这个失败模式）；创建虚拟环境后还会复查一次，确认真的建成了；启动服务也用虚拟环境里的 python，而不是碰运气看 PATH。
- 新增**环境体检**（`backend/tools/env_report.py`）：启动脚本开跑前会先体检，把「Python 版本过低」「当前 Python 缺少 venv / ensurepip（嵌入式 Python，ComfyUI 整合包里就是这种）」「项目被放在 ComfyUI 的 custom_nodes 里」「Node.js / npm 缺失」「端口被占用」这些一次讲清楚；任何一项判错就中止启动，而不是继续往下撞。用系统 Python 跑体检时，它还会换成虚拟环境的解释器复查一遍依赖是否装全。

### 新增

- **诊断报告**：`python backend/tools/env_report.py --report` 会输出一份可整段复制发送的报告（解释器版本与路径、依赖是否齐全、端口占用、数据目录大小等）。**报告只含环境信息，不含任何 API Key、密钥或数据库内容**，遇到问题可以直接发给作者。
- **README 补「下载项目」一节**：GitHub / Gitee 的克隆地址、SSH 写法、只拉单个发行版的写法、不用 git 的 ZIP 直链，并说明**推荐 git clone 的理由**——应用内「一键更新」只在 git 安装下可用。
- 明确写出**不要把本项目放进 ComfyUI 的 `custom_nodes` 目录**：那不是插件而是独立服务，放进去会让 ComfyUI 尝试加载它，并且整合包自带的嵌入式 Python 无法创建虚拟环境。

### 说明

- 功能无变化，无数据变更。已经能正常启动的用户不受影响，但**建议升级**：以后启动失败会直接告诉你原因。

## v1.1.1 · 2026-09-16

修一个只在「更新源选 Gitee」时才出现的小问题：有新版时，更新提示里的 Release 链接是空的。

### 修复

- **Gitee 更新源的 Release 链接缺失**：Gitee 的 Release 接口不返回页面地址（`html_url` 恒为 `null`，GitHub 会返回），此前直接把空值透传给界面，于是「查看 Release 说明」这个链接在**检测到新版本时会凭空消失**。现在改成「接口给了就用接口的，没给就按平台规则拼出 `仓库/releases/tag/标签`」；若信息不足以拼出有效地址，宁可留空，也不给出一个打不开的链接。

### 说明

- 无新增功能、无数据变更，升级方式与 v1.1.0 相同；本次修复对已在使用 GitHub 更新源的用户没有可见影响。

## v1.1.0 · 2026-09-16

从「跑得通」到「出得了片」：这一版把自动链补成了完整流水线，并让中间每一步都看得见、改得动。

### 新增

- **资产链（自动链 L2）**：新增「资产表」与「资产设定图」两个节点。资产表把剧本/分镜里出现的角色、场景、道具整理成一张 Markdown 表格（**可以直接手改**）；资产设定图逐行出图——角色出三视图 + 面部特写、场景出广角空镜、道具出多角度产品图，每张图都带着**资产名**进资产库。
- **角色提及注入**：下游图片 / 视频节点的提示词里只要**提到资产名**（例如「暮光闪闪 站在永恒花园的门口」），执行时就自动把对应设定图挂成参考图——同项目内精确匹配、长名优先、一次最多 3 张，首页产物不重复挂，可在浮框里关掉。
- **导演风格库**：内置 12 张风格卡（8 位导演技法 + 国漫 / 赛博朋克 / 水墨 / 纪实手持）。在小说 / 剧本 / 分镜 / 分镜图 / 视频节点上选一次「风格」，它会自动注入三个位置：创作 Agent 的系统提示词、生图提示词的英文技法片段、生视频提示词的运镜片段。风格卡可在「系统设置 → 导演风格」里改，改完立即生效；浮框里的「同步全链」可一次性套到所有节点。
- **分镜图节点（自动链 L3）**：读分镜表**逐镜出图**，每镜用它自己的「首帧提示词」（缺失则退回画面描述），并自动追加所选风格的技法词；**每一镜单独做一次角色提及注入**，只挂这一镜提到的那几个角色，而不是把整批设定图全塞进去。可设「生成镜数」（0 = 全部，上限 24 镜），先用两三镜试出味道再跑全片。
- **正文可以在画布上直接改**：任一文档节点（创意 / 小说 / 剧本 / 分镜 / 资产表）的浮框里点「编辑」，打开大弹窗改正文，改完点「保存到节点」——**下游读的就是你改过的这份，不必重跑整条链**。节点上会挂「正文已手改」标记，弹窗里可一键还原成生成结果。
- **产物网格**：一次出多张时，浮框里按网格展示产物，可拖动缩略图大小（会记住），每格标出镜号或资产名，点开可放大。

### 改进

- 自动链新增 **L2 / L3 档位**，一键铺链会连好分叉拓扑（分镜与资产设定图同时指向分镜图）。
- 节点浮框重做：横向拉宽、内容超高时内部滚动、底部「运行此节点」常驻吸底；浮框超出画布可视区时会自动平移让位，节点在画布任何位置都能看全。
- 左侧「元素」列表点击现在会**选中并居中**该节点（此前只会高亮，视口外的节点等于够不着）；新增节点或一键铺链后也会直接选中并打开浮框。
- 批量节点（资产表 / 分镜图）的产物归集与节点状态：一次运行多个任务时节点能一次显示整批产物，且只要还有一镜在跑就显示「运行中」，不会因为最后一个任务先完成就误报完成。

### 修复

- **生成完了还显示「排队中」**：后端写库用的是 `completed`，前端状态表里只有 `succeeded`，查不到就兜底成了「排队中」——已出图的卡片状态全错，任务中心「已完成」统计恒为 0。现已对齐，并新增跨层契约测试（扫描后端所有状态写入值 + 解析前端状态表，两边对不上就直接报错），防止再次静默漂移。
- 文档节点的正文预览改为取「批次里第一个文档产物」，不再依赖产物顺序。

### 合规说明

导演风格库里**导演名只出现在给 LLM 的提示词中**（作用是帮模型理解技法）；注入到生图 / 生视频模型的片段一律只写技法特征，不出现导演人名。这条边界落在数据层（拆成不同字段），并有单测把关。

### 已知限制

- ComfyUI 产物目前只归档图片与视频；音频等其它类型会明确报错而不是静默丢弃。
- 视频生成的时长 / 画幅等参数支持情况以具体模型为准，不支持的参数会被上游忽略。
- 资产设定图 / 分镜图一次最多 24 张，防止误填把额度烧光。

## v1.0.0 · 2026-09-15

首个公开版本。一个部署在自己电脑上的个人 AI 创作工作台：填一个接口地址和 API Key，就能接入任意模型。

### 新增

- **BYOK 模型接入**：OpenAI 兼容协议（OpenAI / DeepSeek / Kimi / 通义千问 / 智谱 / OpenRouter / 本地 Ollama）、火山方舟 Ark（Seedream 图片、Seedance 视频）、DashScope（Kling / wan 系列）。密钥本地加密存储，不经任何第三方中转。
- **可视化画布**：ReactFlow 节点式编排，8 类节点（文本 / 创意策划 / 小说 / 剧本 / 分镜 / 图片生成 / 视频生成 / ComfyUI 工作流）。拖拽连线按端口类型实时校验（防环、防类型不匹配），单节点或整图一键执行，上游产物自动作为下游输入。
- **自动链**：一句话创意到分镜的完整流水线。工具栏「自动链」一键铺好四个文档节点；每个阶段是一个可在「系统设置 → 创作 Agent」里修改的 Agent；**长文自动分块续写**（先出大纲再逐章/逐场/逐镜生成），避免被模型输出上限截断。
- **ComfyUI 工作流接入**：上传 `workflow_api.json` 后自动发现可调参数（模型 / 步数 / CFG / 采样器 / 宽高 / 帧数 / 提示词 / 种子 / 参考图），画布浮框按参数表动态渲染表单，运行时可直接调分辨率、步数、张数。
- **在线更新**：内置检查更新与一键更新（`git pull` → 依赖清单变了才装依赖 → 前端有改动才重建）。更新源支持 Gitee / GitHub，双源自动回退；Docker 与压缩包安装会给出对应的升级做法。
- **导演台**：本地视频粗剪——导入视频、标记入/出点、FFmpeg 截取片段、时间线排序、合并导出。
- **创作辅助**：文本对话（SSE 流式）、图片生成（文生图 / 参考图 / 批量对比）、视频生成（文生视频 / 首尾帧 / 全能参考）、提示词库、任务中心、资产库。

### 配置驱动

站点信息、参数档位、能力类型、导航菜单、服务商预设、提示词库、创作 Agent 全部存在数据库里，由「注册制」统一管理——改完即时生效，新增一张配置表会自动获得增删改查、乐观锁、审计与一键回滚，前端也自动多出一个设置页签，不需要改代码。

### 已知限制

- ComfyUI 产物目前只归档图片与视频；音频等其它类型会明确报错而不是静默丢弃。
- 文档节点暂不支持在画布内直接编辑生成的正文（改内容靠调整「补充要求」重跑，或直接改资产库里的 `.md`）。
- 视频生成的时长/画幅等参数支持情况以具体模型为准，不支持的参数会被上游忽略。

### 许可

[PolyForm Noncommercial 1.0.0](./LICENSE)：个人使用免费，禁止商业用途。
