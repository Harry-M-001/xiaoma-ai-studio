# 更新日志

本项目遵循「一个版本 = 一个 git tag + 一份 GitHub/Gitee Release」。应用内的「系统设置 → 关于与更新」读的就是 Release 里的说明。

## v1.1.6 · 2026-09-17

这一版是**拿真实 Key 试出来**的两处修补，外加把「保存失败」那句没头没尾的报错说清楚。

### 修复

- **火山方舟的 Key 认不出来**：方舟现在发的 Key 是 `ark-<uuid>-<尾码>`，而识别规则照的是老版的裸 UUID。把真 Key 粘进「粘贴一个 Key 就接入」，应用认不出归属，还会**拿这串 Key 去试 OpenAI / DeepSeek / Kimi 三家**（当然都是 401）。现在两种形状都认；已经在用的安装由迁移 `0013` 自动更新这条规则。
- **认不出归属时不再瞎试**：以前会按顺序挑三家试一遍——既白等五秒，更要紧的是**把用户的凭据发给了三家他并没有在用的公司**。现在认不出就直接把候选列出来让用户挑，一个请求都不发。
- **「保存失败：存在重复的唯一标识，请修改后重试」说不清是哪一项**：这是配置表（提示词库 / 参数选项 / 服务商预设 / 导演风格 / 创作 Agent / 系统配置）里最常见的保存失败。现在会明确说：「「标识」「xxx」已经被另一行占用了（提示词库里不能重复）。换一个标识再保存；如果想改的是已有那一行，请到列表里直接编辑它。」表单里也在「标识」下方标注了「同一张表里不能重复」。

### 说明

- 数据库多一条迁移（`0013`），启动时自动执行。

## v1.1.5 · 2026-09-17

这一版专治「下载之后到跑通之前」：第一次打开应用最容易卡住的几个点——没有模型、不知道该填什么、不知道能做什么——各自给一条立刻能走通的路；并给出 Windows 解压即用的便携包。

### 新增

- **首次进入的引导横幅**：还没有接入任何模型时，界面顶部直接说明「为什么现在什么都用不了」，并给出两条立刻可走的路（粘贴 Key / 接入本机 Ollama）。已经有服务、只是缺某一类模型（例如缺视频模型）时降级成提醒。关闭状态按「当前缺什么」记下来：将来情况变了，该提示的仍然会提示。
- **粘贴一个 Key 就接入**：「模型服务」页顶部贴一个 Key，应用按预设里的识别规则认出是哪家，用预设地址发一次**真实请求**，**通了才保存**。认不出来时不猜——把候选列出来让你挑（DeepSeek 与通义千问的 Key 形状完全相同，机器分不开是正常的），测不过也留了手动保存的出口。
- **检测并接入本机 Ollama**：探测 `127.0.0.1:11434`，把已经下载好的对话模型一键接进来，不需要任何云账号。向量模型与重排模型会被过滤掉——它们不能对话，混在下拉里只会让人选了之后报一个看不懂的错。
- **内置示例项目**：「从示例开始」会**按你现有的模型自动选档位**——只有文本模型就铺 L1 文本链（创意→小说→剧本→分镜），有图片模型才铺到资产链。第一个节点里写好了一句创意，打开就能点「运行整图」。
- **Windows 便携包**：`xiaoma-ai-studio-v1.1.5-win64-portable.zip`，解压后双击 `start.bat` 即可，**不需要先装 Python**（包内自带运行时与依赖，用户机器上不编译、不联网装包）。打包脚本是 `backend/tools/make_portable.py`。

### 改进

- **上游错误按状态码给出可照做的下一步**：401 提示核对 Key 与服务商是否配套、404 提示 Base URL 该填到哪一级、429 提示看余额与配额、5xx 提示稍后重试。网络类错误也分成了「连不上 / 超时 / 代理」三种说法，而不是笼统一句「网络请求失败」。
- 服务商预设多了「Key 识别规则」一列（可在「系统设置 → 服务商预设」里改），再加一家能从 Key 形状上认出来的服务商不必改代码。
- 接入完成后顶部横幅就地刷新（此前要切一次页面才会消失）。

### 修复

- **示例项目存下来了却打不开**：画布内部所有节点统一用 `contract` 这个渲染类型，而示例把内部名字原样存进了库，后端判「未知节点类型」——前端只看到一句报错，画布一片空白。现在画布与示例共用同一份「落库前整形」。
- **本机 Ollama 被误判成「没装」**：探测超时原本是 1.5 秒，而 Ollama 的 `/api/tags` 实测在 0.6–2.6 秒之间波动，于是**跑着的服务偶尔被判成不存在**——而「本机有 Ollama」正是第一次打开应用时最有用的一条信息。超时放宽到 4 秒（真没装时连接会立刻被拒，不会白等）。
- **只有 Ollama 时示例被挡在门外**：上一版示例固定铺 L2，需要图片模型，而「没有云 Key、只有本机 Ollama」恰恰是最典型的新手状态。

### 说明

- 数据库有一条迁移（`0012`，给服务商预设加 Key 识别规则），启动时自动执行。
- 便携包与源码包的区别、升级时怎么保留数据，见 `docs/portable.md`。
- 已知限制：像 OpenRouter 这类 `/models` 公开的服务商，连通测试无法校验 Key 是否有效（上游本来就不校验），这类 Key 的真正校验会发生在第一次生成时。

## v1.1.4 · 2026-09-17

这一版补上**自动链的最后一段**：分镜图之后能直接逐镜出视频，镜头之间接得上。顺带把任务中心里最常点的两个动作（重新生成、打开目录）做扎实。

### 新增

- **逐镜出视频**：video 节点多了「逐镜出视频」三档——`关闭`（老行为，整段出一个视频）、`逐镜各出一段`、`逐镜并接首尾帧`。开启后，**分镜表里有几个镜就派几个视频任务**，与分镜图节点同构。
- **每镜用自己那张分镜图当首帧**：逐镜配对**按镜号**而不是按顺序——上游正文被截断或补写过时，按顺序配会把别的镜头的图当首帧用出去。
- **同场景串联**（`逐镜并接首尾帧` 时生效）：某一镜的尾帧自动用**下一镜的首帧**，末镜不编造尾帧。另可开启「同场景参考串联」，把**同场景上一镜**接成参考，保证同一场戏里镜头之间接得上；跨场景不串（换场本来就该断开）。
- **逐镜时长**：分镜表里那一镜写了时长（如 `5s`）就用它——4 秒的镜头不该被拉成节点上设的 5 秒；没写才退回节点设置。
- **分镜表场景分组**：认 `## 场景2 | 黄昏的花园` 这类标题（`场景`/`幕`/`scene`/`act` 都认），标题没编号也能归组，不会散成一片。
- **任务「打开目录」**：任务卡片上有产物时多一个按钮，直接在文件管理器里定位到产物文件。
- **重新生成（原来的「重试」）**：不再让你回忆上次怎么配的——**沿用当时的参数快照**，只覆盖你在弹窗里显式改的项；按钮上会标「重跑自 #N」，任务中心能区分首次与重跑。重跑出来的任务**仍挂在原画布节点上**，产物会回到画布。
- **`--issue` 模式**：`python backend/tools/env_report.py --issue` 直接吐出**按 Issue 模板排好版**的内容，补三句话就能提交；「导出日志」弹窗里也多了「Issue 内容」页签。仓库里加了 `.github` / `.gitee` 的 Issue 模板。
- **`start.bat` 加 uv 兜底**：虚拟环境建不出来时（嵌入式 Python 没有 `venv` 模块）会再试 `uv venv --seed`，这是第三道回退。

### 修复

- **重复镜号会重复出片，费用翻倍**：上游正文有可能是多份分镜表拼起来的（重跑过上游节点，或手工粘了两份）。此前会按镜号重复派任务——3 镜变 9 段，**账单三倍，而任务数量上完全看不出异常**。现在分镜图与视频两侧都按镜号去重，并把去重数量写进日志。
- **短视频时长被解析错**：分镜表里写 `120s`（明显是笔误）会被读成 `12` 秒拿去出片。数字两侧加了边界，超出 1–30 秒范围一律视为未填。
- **「重新生成」早先只认图片通道**：视频、ComfyUI、文档类任务重跑会被当图片任务跑。现在按任务类型分发。
- **上游返回的错误正文可能把提示词带回来**：任务日志里现在只记结构化摘要（HTTP 状态、错误类型、响应体字节数）。

### 说明

- 数据库有一条迁移（`0011`，给任务加「重跑血缘」字段），启动时自动执行。
- 现有画布：video 节点默认仍是「关闭」，不改变已有作品的行为；想要逐镜出片需要在节点上手动开启。

## v1.1.3 · 2026-09-16

这一版做「出问题的时候，你能自己看清发生了什么，也能一键把该给的信息给出来」，同时把日志里的隐私收干净。

### 新增

- **日志落盘**：日志现在同时写一份到 `backend/data/logs/app.log`（单个文件上限 5 MB，保留 5 份，自动轮转）。此前只输出到控制台，窗口一关就什么都没有了。
- **导出诊断报告**：「系统设置 → 关于与更新 → 日志与诊断」里点「导出日志」，会生成一份报告——环境体检结论 + 最近 200 条错误与警告 + 日志文件清单。可以先在界面上完整看一遍，再「复制全文」或「下载 .txt」发给作者。也可以直接访问 `GET /api/logs/export.txt` 拿到同一份内容。
- **任务自己的日志**：任务中心的每张卡片多了「日志」按钮，能看到这个任务在执行期间产生的全部日志。多数问题看这里就够了，不必来问。

### 改进

- **日志写盘前先脱敏**：接口密钥、URL 查询参数、系统用户名与家目录路径会被替换成占位符。也就是说**日志文件本身就是可以安全外发的产物**，不存在「导出时忘了脱敏」这条路径。控制台保留完整信息，方便本机排查。
- **上游返回的正文不再进日志**：为了让用户看清原因，错误消息里会带上游返回的错误正文；但上游在 400 时经常把请求体回显回来，那里面就是用户的提示词。现在这类错误额外带一份枚举化的安全摘要（HTTP 状态、错误类型、响应体字节数），日志只记摘要、不记正文。这条在数据层做成强制的：拿不到安全摘要时日志会显式标注「已省略消息」，而不是退回去打印堆栈。

### 修复

- **日志一条都写不出去（静默失效）**：文件 handler 用了延迟打开，而重写的写入方法漏了补上文件句柄，结果每次写入都在内部报错并被吞掉，界面上完全看不出异常。
- **服务启动后的日志全部丢失**：数据库迁移（Alembic）会用自带的日志配置**替换掉根 logger 的全部 handler**，把刚装配好的控制台与文件 handler 一起冲掉。现在迁移不再接管已有配置，启动流程也会在迁移之后重新装配一次。
- **脱敏会把日志本身弄坏**：先洗格式串再插值，会把 `%s` 之类的占位符一起洗掉，于是插值时参数比占位符多，直接抛错、整条日志丢失。现在改成先插值再脱敏。
- 上游返回空响应体时，错误消息不再留下一个孤零零的冒号。

### 说明

- 日志目录在 `data/` 下，不会被「一键更新」影响。任务日志只保留在内存（最多 50 个任务 × 1000 行），服务重启后可在导出的报告里看完整记录。

## v1.1.2 · 2026-09-16

修「一键启动脚本卡在某一步、但看不出为什么」这个体验问题，并把安装说明补全。

### 修复

- **启动脚本对每一步都不检查成败，失败也继续往下跑**：有用户把项目放在 ComfyUI 的 `custom_nodes` 目录里，`start.bat` 的「安装后端依赖」这一步反复失败，而控制台只有一堆无从下手的红字。根因是当时的脚本不检查返回码——尤其是 `python -m venv` 失败时会**静默跳过**，紧接着 `call activate.bat` 找不到文件，pip 就装到了错误的解释器上，表现成「第二步一直失败」。
- 现在的启动脚本：每一步都检查返回码，失败立刻停下并打印可能的原因与下一步动作；不再依赖 `activate.bat` 切换解释器，而是**直接用虚拟环境里的 python 绝对路径**（少了「静默切不过去」这个失败模式）；创建虚拟环境后还会复查一次，确认真的建成了；启动服务也用虚拟环境里的 python，而不是碰运气看 PATH。
- 新增**环境体检**（`backend/tools/env_report.py`）：启动脚本开跑前会先体检，把「Python 版本过低」「当前 Python 缺少 venv / ensurepip（嵌入式 Python，ComfyUI 整合包里就是这种）」「项目被放在 ComfyUI 的 custom_nodes 里」「Node.js / npm 缺失」「端口被占用」这些一次讲清楚；任何一项判错就中止启动，而不是继续往下撞。用系统 Python 跑体检时，它还会换成虚拟环境的解释器复查一遍依赖是否装全。

### 新增

- **诊断报告**：`python backend/tools/env_report.py --report` 会输出一份可整段复制发送的报告（解释器版本与路径、依赖是否齐全、端口占用、数据目录大小等）。**报告只含环境信息，不含任何 API Key、密钥或数据库内容**，遇到问题可以直接发给作者。
- **README 补「下载项目」一节**：GitHub / Gitee 的克隆地址、SSH 写法、只拉单个发行版的写法、不用 git 的 ZIP 直链，并说明**推荐 git clone 的理由**——应用内「一键更新」只在 git 安装下可用。
- 明确写出**不要把本项目放进 ComfyUI 的 `custom_nodes` 目录**：那不是插件而是独立服务，放进去会让 ComfyUI 尝试加载它，并且整合包自带的嵌入式 Python 无法创建虚拟环境。

### 说明

- 功能无变化，无数据变更。已经能正常启动的用户不受影响，但**建议升级**：以后启动失败会直接告诉你原因。

## v1.1.1 · 2026-09-16

修一个只在「更新源选 Gitee」时才出现的小问题：有新版时，更新提示里的 Release 链接是空的。

### 修复

- **Gitee 更新源的 Release 链接缺失**：Gitee 的 Release 接口不返回页面地址（`html_url` 恒为 `null`，GitHub 会返回），此前直接把空值透传给界面，于是「查看 Release 说明」这个链接在**检测到新版本时会凭空消失**。现在改成「接口给了就用接口的，没给就按平台规则拼出 `仓库/releases/tag/标签`」；若信息不足以拼出有效地址，宁可留空，也不给出一个打不开的链接。

### 说明

- 无新增功能、无数据变更，升级方式与 v1.1.0 相同；本次修复对已在使用 GitHub 更新源的用户没有可见影响。

## v1.1.0 · 2026-09-16

从「跑得通」到「出得了片」：这一版把自动链补成了完整流水线，并让中间每一步都看得见、改得动。

### 新增

- **资产链（自动链 L2）**：新增「资产表」与「资产设定图」两个节点。资产表把剧本/分镜里出现的角色、场景、道具整理成一张 Markdown 表格（**可以直接手改**）；资产设定图逐行出图——角色出三视图 + 面部特写、场景出广角空镜、道具出多角度产品图，每张图都带着**资产名**进资产库。
- **角色提及注入**：下游图片 / 视频节点的提示词里只要**提到资产名**（例如「暮光闪闪 站在永恒花园的门口」），执行时就自动把对应设定图挂成参考图——同项目内精确匹配、长名优先、一次最多 3 张，首页产物不重复挂，可在浮框里关掉。
- **导演风格库**：内置 12 张风格卡（8 位导演技法 + 国漫 / 赛博朋克 / 水墨 / 纪实手持）。在小说 / 剧本 / 分镜 / 分镜图 / 视频节点上选一次「风格」，它会自动注入三个位置：创作 Agent 的系统提示词、生图提示词的英文技法片段、生视频提示词的运镜片段。风格卡可在「系统设置 → 导演风格」里改，改完立即生效；浮框里的「同步全链」可一次性套到所有节点。
- **分镜图节点（自动链 L3）**：读分镜表**逐镜出图**，每镜用它自己的「首帧提示词」（缺失则退回画面描述），并自动追加所选风格的技法词；**每一镜单独做一次角色提及注入**，只挂这一镜提到的那几个角色，而不是把整批设定图全塞进去。可设「生成镜数」（0 = 全部，上限 24 镜），先用两三镜试出味道再跑全片。
- **正文可以在画布上直接改**：任一文档节点（创意 / 小说 / 剧本 / 分镜 / 资产表）的浮框里点「编辑」，打开大弹窗改正文，改完点「保存到节点」——**下游读的就是你改过的这份，不必重跑整条链**。节点上会挂「正文已手改」标记，弹窗里可一键还原成生成结果。
- **产物网格**：一次出多张时，浮框里按网格展示产物，可拖动缩略图大小（会记住），每格标出镜号或资产名，点开可放大。

### 改进

- 自动链新增 **L2 / L3 档位**，一键铺链会连好分叉拓扑（分镜与资产设定图同时指向分镜图）。
- 节点浮框重做：横向拉宽、内容超高时内部滚动、底部「运行此节点」常驻吸底；浮框超出画布可视区时会自动平移让位，节点在画布任何位置都能看全。
- 左侧「元素」列表点击现在会**选中并居中**该节点（此前只会高亮，视口外的节点等于够不着）；新增节点或一键铺链后也会直接选中并打开浮框。
- 批量节点（资产表 / 分镜图）的产物归集与节点状态：一次运行多个任务时节点能一次显示整批产物，且只要还有一镜在跑就显示「运行中」，不会因为最后一个任务先完成就误报完成。

### 修复

- **生成完了还显示「排队中」**：后端写库用的是 `completed`，前端状态表里只有 `succeeded`，查不到就兜底成了「排队中」——已出图的卡片状态全错，任务中心「已完成」统计恒为 0。现已对齐，并新增跨层契约测试（扫描后端所有状态写入值 + 解析前端状态表，两边对不上就直接报错），防止再次静默漂移。
- 文档节点的正文预览改为取「批次里第一个文档产物」，不再依赖产物顺序。

### 合规说明

导演风格库里**导演名只出现在给 LLM 的提示词中**（作用是帮模型理解技法）；注入到生图 / 生视频模型的片段一律只写技法特征，不出现导演人名。这条边界落在数据层（拆成不同字段），并有单测把关。

### 已知限制

- ComfyUI 产物目前只归档图片与视频；音频等其它类型会明确报错而不是静默丢弃。
- 视频生成的时长 / 画幅等参数支持情况以具体模型为准，不支持的参数会被上游忽略。
- 资产设定图 / 分镜图一次最多 24 张，防止误填把额度烧光。

## v1.0.0 · 2026-09-15

首个公开版本。一个部署在自己电脑上的个人 AI 创作工作台：填一个接口地址和 API Key，就能接入任意模型。

### 新增

- **BYOK 模型接入**：OpenAI 兼容协议（OpenAI / DeepSeek / Kimi / 通义千问 / 智谱 / OpenRouter / 本地 Ollama）、火山方舟 Ark（Seedream 图片、Seedance 视频）、DashScope（Kling / wan 系列）。密钥本地加密存储，不经任何第三方中转。
- **可视化画布**：ReactFlow 节点式编排，8 类节点（文本 / 创意策划 / 小说 / 剧本 / 分镜 / 图片生成 / 视频生成 / ComfyUI 工作流）。拖拽连线按端口类型实时校验（防环、防类型不匹配），单节点或整图一键执行，上游产物自动作为下游输入。
- **自动链**：一句话创意到分镜的完整流水线。工具栏「自动链」一键铺好四个文档节点；每个阶段是一个可在「系统设置 → 创作 Agent」里修改的 Agent；**长文自动分块续写**（先出大纲再逐章/逐场/逐镜生成），避免被模型输出上限截断。
- **ComfyUI 工作流接入**：上传 `workflow_api.json` 后自动发现可调参数（模型 / 步数 / CFG / 采样器 / 宽高 / 帧数 / 提示词 / 种子 / 参考图），画布浮框按参数表动态渲染表单，运行时可直接调分辨率、步数、张数。
- **在线更新**：内置检查更新与一键更新（`git pull` → 依赖清单变了才装依赖 → 前端有改动才重建）。更新源支持 Gitee / GitHub，双源自动回退；Docker 与压缩包安装会给出对应的升级做法。
- **导演台**：本地视频粗剪——导入视频、标记入/出点、FFmpeg 截取片段、时间线排序、合并导出。
- **创作辅助**：文本对话（SSE 流式）、图片生成（文生图 / 参考图 / 批量对比）、视频生成（文生视频 / 首尾帧 / 全能参考）、提示词库、任务中心、资产库。

### 配置驱动

站点信息、参数档位、能力类型、导航菜单、服务商预设、提示词库、创作 Agent 全部存在数据库里，由「注册制」统一管理——改完即时生效，新增一张配置表会自动获得增删改查、乐观锁、审计与一键回滚，前端也自动多出一个设置页签，不需要改代码。

### 已知限制

- ComfyUI 产物目前只归档图片与视频；音频等其它类型会明确报错而不是静默丢弃。
- 文档节点暂不支持在画布内直接编辑生成的正文（改内容靠调整「补充要求」重跑，或直接改资产库里的 `.md`）。
- 视频生成的时长/画幅等参数支持情况以具体模型为准，不支持的参数会被上游忽略。

### 许可

[PolyForm Noncommercial 1.0.0](./LICENSE)：个人使用免费，禁止商业用途。
